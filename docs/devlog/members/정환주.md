# 개발일지 — 정환주 (AI 엔지니어)

> 최신 날짜를 **맨 위**에 추가하세요. 통합 일지: [../README.md](../README.md)
>
> <details><summary>📋 작성 템플릿 (복사해서 사용)</summary>
>
> ```markdown
> ## YYYY-MM-DD (Day N)
> ### 오늘의 목표
> -
> ### 한 일
> - [x]
> ### 막힌 것 / 고민
> -
> ### 내일 할 일
> - [ ]
> ```
> </details>

---

## 2026-06-16 (Day 16) — TTS 로그분석기·STT 발음평가 + Qwen3 운영 pm2 전환·실행 가이드

> 06-16~17 이틀치를 한 항목으로 정리.

### 오늘의 목표
- TTS 운영 가시성(로그·엔진비용)·품질(발음) 객관 측정 도구 마련 + Qwen3 GPU 서버 운영 정리(단일화·인수인계 문서)

### 한 일
- [x] **TTS 로그 분석기 신설** (`ai/tts/analyze_tts_log.py`) — **실행 중인 서버는 안 건드리고**(read-only `GET /health`만) uvicorn 액세스 로그를 파싱해 요청 수·시간대·상태코드 집계 + **응답 엔진 호출 요약**(사용량·1콜 평균 비용·엔진 종류: `Qwen3 로컬·무료` vs `Google 폴백·유료`) → **CSV(Excel용 utf-8-sig)·Markdown 제출 리포트** 동시 출력. `--watch` 라이브 모드(health 폴링→CSV) 포함
- [x] **인코딩 함정 2건 수정** — ① 콘솔 `cp949` UnicodeEncodeError(em-dash·이모지) → stdout/stderr UTF-8 reconfigure ② `start_tts.bat` 로그 저장이 PowerShell 5.1 `Tee-Object`(UTF-16)면 분석기가 깨짐 → **pwsh7 고정(UTF-8 no-BOM)** + `python -u`(버퍼링 방지) + `-Append`(이전 로그 누적)
- [x] **TTS 발음 명료도 객관 평가** (`ai/tts/analyze_stt.py`) — faster-whisper(large-v3) STT 받아쓰기 → 원문 대비 **CER + WER** 산출, **제출용 JSON `{"cer","wer"}`** 출력. 최종 확정 엔진 **Google = CER 0.0% / WER 9.1%**(WER은 "남아 있어요→남아있어요" 띄어쓰기 차이 아티팩트, 실오류 아님), ElevenLabs 9종 비교 동봉
- [x] **PR #336 리뷰·승인** — 교차검증 후 approve (06-16 머지)
- [x] **★Qwen3 TTS 운영 단일화 = pm2 전환** — 세종 요청·팀 합의로 8002 단일 서버를 pm2 관리화. `ecosystem.config.js`(+런처 `pm2_qwen3.bat`). **Windows에서 pm2가 `.bat` 직접 spawn 시 `EINVAL` → `cmd.exe /c` 래퍼로 해결.** `pm2 restart qwen3` **실측 검증**(옛 python 정상 종료·GPU compute 단일·VRAM 6.1/8.1GB 이중점유 없음·`degraded` 해제). 처음 의심한 conda tree-kill orphan은 conda-script shim 오카운트였음(실제 누수 없음)
- [x] **운영 통제권은 수동으로 환원** — pm2가 자동으로 띄워둔 상태가 혼란 → 본인이 직접 켜는 방식으로 결정, pm2 인스턴스 `delete`(현재 자동관리 안 함, 수동 `pm2 start`/`stop`)
- [x] **TTS 실행 인수인계 가이드** (`ai/tts/TTS_실행가이드.md`) — 서버(pm2)→터널(cloudflared)→백엔드 URL 등록(quick tunnel이 켤 때마다 바뀌는 문제 대응) **A→B→C 순서** + 끄기·자주쓰는 명령·트러블 대처를 비개발자도 따라치게 정리
- [x] **8003 워치독 제거 결정 기록** (`ai/tts/TTS_8003_워치독_해결_260616.md`) — 옛 워치독이 2번째 서버(8003)를 띄워 **VRAM 이중점유** → 워치독 영구 제거

### 막힌 것 / 고민
- pm2 `windowsHide`가 conda 콘솔엔 안 먹어 **서버 창이 보임**(기능은 정상, 보기에만 거슬림)
- Windows+conda+pm2 tree-kill·orphan 리스크 → 검증상 현재는 깨끗하나, 운영 중 재시작 시 `nvidia-smi`로 잔류 python 1회 확인 권장
- 자동복구(autorestart/워치독)는 VRAM 이중점유 이력 때문에 보수적 → **수동 운영**으로 갈음(통제권 우선)

### 내일 할 일
- [ ] cloudflared **고정 도메인(named tunnel)** 검토 — 매번 URL 재등록 제거 가능한지
- [ ] ④ TTS E2E 음성재생 최종 검증

---

## 2026-06-15 (Day 15) — 누적 작업 dev 머지(PR #285) + Qwen3 TTS 운영 안정화

### 오늘의 목표
- 미커밋이던 평가·TTS·PII·삼성헬스 작업을 dev에 일괄 반영 + Qwen3 TTS 운영 점검

### 한 일
- [x] **누적 작업 dev 머지 (PR #285)** — 평가(삼성헬스 export 파서·회복점수), TTS(`server.py` 비동기 잡·hang 가드), PII 비식별(`pii.py`+`safety.py` 입력분리), 삼성헬스 핸드오프를 일괄 커밋(`779666a`)·머지
- [x] **Qwen3 TTS ↔ NCP 통합 트러블슈팅 종합 문서** 작성 (`ai/tts/Qwen3_TTS_트러블슈팅.md`) — 전체 에러 모드·원인·해결 단일 기록
- [x] **TTS GPU 서버 운영 점검** — 8003(옛 watchdog 자동화)과 8002 수동 서버가 동시 구동돼 VRAM(8GB) 충돌 진단. 운영방식 협의 결과 8002 단일화는 직접 진행키로 하고, 현 자동화(B안) 유지로 원복

### 막힌 것 / 고민
- 8003 자동화 ↔ 8002 수동 운영 **이원화** → 단일화 정리 필요
- VRAM 8GB 헤드룸 빠듯 → hang 만성 리스크 지속(폴백으로 소리는 유지)

### 내일 할 일
- [ ] TTS 운영 8002 단일화 직접 정리
- [ ] ④ TTS E2E 음성재생 최종 검증

---

## 2026-06-14 (Day 14) — TTS 트러블슈팅 종합 런북 + 600자 폴백 진짜원인 확정

### 오늘의 목표
- TTS "안 나옴" 잔여 증상(긴 메시지 폴백)의 진짜 원인 규명 + 흩어진 진단을 단일 런북으로 정리

### 한 일
- [x] **TTS 트러블슈팅 종합 런북 작성** (`ai/tts/TTS_트러블슈팅_종합_260614.md`) — 단계별로 흩어진 에러 모드·원인·해결을 단일 출처로 통합
- [x] **🔴 600자 메시지 폴백 진짜원인 확정** — `_SYNTH_TIMEOUT=150s`가 실제 합성시간(600자≈200s)보다 짧아 **깨끗한 GPU에서도 긴 메시지를 무조건 타임아웃→폴백**시키던 버그. 기존 「환경벽(VRAM)」 오진단을 코드 상수 실측으로 정정 → `150→300s` 상향, 실서버 586자=done(200s) 검증 ✅

### 막힌 것 / 고민
- 큰 결론(환경 탓)으로 점프하기 전 **실제 코드 상수부터 읽었어야** — 오진단 교훈

### 내일 할 일
- [ ] 누적 작업 dev 일괄 반영 + Qwen3 트러블슈팅 종합 문서화

---

## 2026-06-13 (Day 13) — TTS 신뢰성 하드닝 + 터널 URL 자동등록

### 오늘의 목표
- TTS가 간헐적으로 "안 나옴" 잔여 원인(500·VRAM hang·휘발성 URL)을 **무인 자동화로 근본 수습**

### 한 일
- [x] **🔴 합성 500 = `WinError5` 잡음** — `qwen3_synthesize.py` `_OUTPUT_DIR` 기본값이 **상대경로**(`"ai/tts/_output"`)라, 자동기동(작업스케줄러/HKCU Run)으로 뜨면 **CWD가 시스템 폴더** → `os.makedirs`가 권한거부. 예전엔 레포 폴더서 수동 실행해 우연히 맞았던 잠복버그. → 기본값을 **모듈 옆 절대경로**로(`TTS_OUTPUT_DIR` env 우선). CWD 무관
- [x] **503/hang 진단** — health=ok·model_ready=true인데 `/synthesize` 503. 원인=**VRAM 746MB만 남아**(8GB를 데스크톱앱과 공유) 합성 job이 `processing`에서 hang → 멈춘 job이 GPU 세마포어+VRAM 영구점유 → 이후 전부 503. 라이브로 재현("테스트" 3자가 140s+ processing·GPU util 1%)
- [x] **server.py hang 가드** — `_SYNTH_TIMEOUT=150s` + `asyncio.wait_for(run_in_threadpool(synthesize),150)`(동기/async 둘 다). 타임아웃 시 job=error(→백엔드 즉시 구글폴백=소리) + 전역 `_degraded=True` → `/health` model_ready=false + 새 합성요청 즉시 503(두번째 생성 차단=죽음의 소용돌이 방지). **advisor 권고**: 스레드는 못 죽이니 sem만 풀면 stuck스레드가 VRAM 쥔 채 2차생성 경합 → degraded로 전체 차단
- [x] **VRAM 단편화 완화** — `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`(torch init 전 setdefault) + 매 합성후 `torch.cuda.empty_cache()`(동기/async 공통경로)
- [x] **★터널 URL 자동등록 B안(완전자동) 설계·구현** — cloudflared quick tunnel URL이 재시작마다 바뀌어 NCP `.env` 수동갱신이 팀 진도를 막던 문제. **watchdog**(`C:\Rainbow_Bridge\tts_url_watchdog.ps1`, 레포 밖)이 30초 루프로 `/quicktunnel` URL 조회·`/tts/health`(model_ready) 검증 → 백엔드 `POST /api/v1/tts/register-url`(시크릿+화이트리스트)로 push → **Redis(TTL 300s)** → `tts.py`가 redis 우선·`.env` 폴백. 핸드오프 [TTS_URL_AUTO_HANDOFF.md](../../../ai/tts/TTS_URL_AUTO_HANDOFF.md) 작성 → **윤한님 PR #268 머지·NCP 재배포·시크릿 합의 완료, 가동 시작**
- [x] **watchdog GPU 자동복구 추가** — URL 등록뿐 아니라 8003 health 보고 `degraded:true`(hang)·무응답 연속3회(크래시) 시 GPU 서버 자동 재시작(`Restart-TtsServer`, 쿨다운 600s). hang→degraded→자동재시작→qwen3복귀 무인루프
- [x] **reviewer 교차검증 반영** — ①TTL 없으면 죽은 URL 영구 잔존→매 TTS 60초 지연(TTL 300s+heartbeat로 해결) ②SSRF 화이트리스트(https+`*.trycloudflare.com`만) ③시크릿 미설정 KeyError→503 ④다중 cloudflared 식별(8003 터널만, 동반사살 방지)

### 막힌 것 / 고민
- **VRAM 8GB를 데스크톱앱과 공유 = hang 만성 리스크.** 폴백(구글)으로 소리는 유지되나 qwen3는 자주 짐. 자동복구가 매번 수습하나 근본은 **헤드룸 확보** 필요(비-GPU앱 닫기 또는 양자화/클라우드)
- GPU 서버(`server.py`)·`qwen3_synthesize.py` 변경은 **깃 미커밋**(정환주 영역, 시킬 때만 PR). 백엔드 측은 윤한 PR #268로 배포됨

### 내일 할 일
- [ ] hang 완화 — 긴 메시지 합성 시 VRAM 피크를 낮추는 방안(문장 청크 등) 검토
- [ ] 헤드룸 빠듯하면 양자화/클라우드 GPU 카드 비교

---

## 2026-06-12 (Day 12) — TTS "안 나옴" 종합 해결 + 비동기 전환

### 오늘의 목표
- 팀 진도 막던 **TTS "안 나옴"**을 단계별 원인까지 전수로 끝내기 + ⑧ 삼성헬스 export 파서(Day 11 이월)

### 한 일
- [x] **TTS "안 나옴" 단계별 원인 전수 진단·해결** ([TTS_상태_핸드오프_260612.md](../../../ai/tts/TTS_상태_핸드오프_260612.md)) — 여러 증상이 따로 떠 "환주냐 터널이냐" 혼선이었는데 실제론 단계별 다른 원인: `status=None`(합성 멈춤→타임아웃)·`404`(8003 직결 후 `/tts` 경로 없음)·`504`(느림)·목소리 동일(백엔드 `voice` 키 vs 서버 `tone` 키 불일치)·**간헐 멈춤**(codec EOS 미방출로 8192토큰 폭주=수백초 hang)
- [x] **★긴 메시지 = 비동기 잡 전환** — 합성이 글자수 비례(~0.33s/자)라 212자=70초 > 백엔드 60초 타임아웃 → `context canceled`→폴백. LivePortrait처럼 **제출(`POST /synthesize/async`)→상태조회(`/status/{id}`)→결과(`/result/{id}`)** 3단 엔드포인트 추가(`server.py`), 각 호출 2초라 길이·터널 무관. 백엔드 `tts.py`도 폴링 전환(윤한 재배포 대상). 기존 `/synthesize` 하위호환 유지
- [x] **멈춤·정지 차단 보강** — `max_new_tokens` 상한(폭주 차단)·ffmpeg atempo 타임아웃·혼잡 시 즉시 503(→구글 폴백)·모델 사전로드(첫 합성 콜드스타트 20초 제거)·경로(`/`·`/tts`)·키(`tone`·`voice`) 양쪽 호환
- [x] **ngrok → cloudflared quick tunnel 교체** (`gpu_server_start.ps1`, 세종 지시) — 소람님 LP와 같은 방식. 무료 제한 없음
- [x] **Qwen3-TTS 공개 API 조사** ([QWEN3_TTS_API_조사_260612.md](../../../ai/tts/QWEN3_TTS_API_조사_260612.md))
- [x] **⑧ 삼성헬스 데이터 export 파서** (`ai/evaluation/load_samsung_export.py` + 테스트 + `health_export_adapter.py`) — 폰 실연동(개발빌드+권한) 없이도 **export 파일 → 회복점수 실데이터**(발표 백업 경로, Day 11 이월)
- [x] **⑦ 위기감지 PII 비식별** (`ai/evaluation/pii.py` + 테스트, `safety.py` 입력분리, `emotion.py`·`message.py` 호출부) — 로그·LLM 전송 시 원문 PII 가림. **reviewer 1차 미탐버그**(redact가 규칙패턴을 깸) → `assess_crisis` **입력 분리**(규칙=원문 미탐0 / LLM=가린 것 전송, fused=max라 안전)로 수정
- [x] **회복점수 재설계 v2 문서** (`회복점수_재설계_260611.md`) — 감정 점수서 빼는 4요소(State/Action/Consistency/LifeChange)·티어·하루 미션 1~3개·졸업 도달 가능안

### 막힌 것 / 고민
- **백엔드 `tts.py`·`server.py` 깃 미푸시** → 윤한님 재배포 전까지 라이브 미적용(긴 메시지 여전히 끊김)
- **cloudflared URL 휘발성** — 재시작마다 바뀌어 `.env` 수동갱신 부담 + 무료터널 uptime 보장 없음·watchdog 부재 → **자동화 필요**(내일)
- PII redact가 규칙 패턴을 깰 뻔 → 입력 분리로 해결(reviewer 교차검증 가치 재입증)

### 내일 할 일
- [ ] URL 휘발성 근본해결 — watchdog 자동등록(검증→백엔드 push→Redis)
- [ ] WinError5 500·VRAM hang 잔여 추적

---

## 2026-06-11 (Day 11)

### 오늘의 목표
- ⑧ 평가 — 삼성헬스 객관 수면·활동 확장 + **PR #228 리뷰(반소람) 반영**: 수면을 회복점수에 넣을지 결판

### 한 일
- [x] **🔑 수면 점수 제외로 결판 (PR #228 리뷰 수용)** — 결정문서 2개가 정면충돌이었음(반소람 §2 "수면 제외" vs 내 §0 "수면 15% 반영"). 둘 다 모세종 권위 인용 → 충돌 표면화 후 **리뷰 수용**: 수면은 회복점수서 빼고 `cross_check`·리포트 표시·미션 난이도로만. (논문은 수면-비애 "연관"까지지 "예측" 아님 → 점수 편입 과함)
- [x] **회복점수 산식 정렬** — `blend_recovery_score`에서 수면 인자 삭제, 핵심 **40/35/25 정본 복원**(감정/미션/꾸준), **활동(걸음)만 +10 add-on**(옵션 A 재정규화 유지). 헬스 미제공 시 기존 `recovery_score` 100% 동일(하위호환)
- [x] **🔴 reviewer가 숨은 버그 발견** — base 미션캡 35 vs blend `_MISSION_CAP` 25 불일치 → 걸음 들어와 base→blend 전환 시 **활동과 무관하게 미션점수만으로 회복점수가 튐**(미션10개 +4). **cap 35 통일** + `test_blend_equals_base_when_no_activity` 회귀가드 추가. (자기 pytest 58통과로는 못 본 것 — 독립 검증 가치 재입증)
- [x] **cross_check 보호자 톤 순화(리뷰 🟡)** — note가 evidence로 보호자에 그대로 노출 → 임상 해석("숨기는 중") 빼고 비낙인 톤만, 임상 의미는 `status` 코드로
- [x] **어댑터 docstring 경고(리뷰 🟡)** — `total_steps`/`total_sleep_hours`가 레코드 무조건 합산 → "단일 일/밤만 질의" 명시
- [x] **검증** — `ai/evaluation` 테스트 **58개 통과**, ruff·black clean. **병렬 reviewer 에이전트**로 코드 독립 검증(🔴블로커 발견·수정) + **문서 검증 에이전트**로 공부노트 사실 대조(4오류 발견·수정 — `_resolve_voice` 동작 반대 묘사 등)
- [x] **삼성헬스 RN 실연동 핸드오프** (`ai/evaluation/HANDOFF_삼성헬스_RN연동.md`) — `react-native-health-connect@3.5.3` **공식 API 검증** 후 RN 코드(readRecords→백엔드 POST)·백엔드 엔드포인트 계약·개발빌드 가이드 작성. RN=민경이, 엔드포인트=모세종 인계
- [x] **데모 실행** — `python -m ai.evaluation.demo_health_report` → ②시나리오 mismatch_high_risk 실증, 수면 "참고-점수 미반영" 표시 확인
- [x] **문서 정정** — `회복점수_삼성헬스_확장_260611.md` §0 "수면 반영"→"제외"로 모순 해소 + 공부노트 `ai/정환주_파트_공부노트_전영역_260611.html`(다크테마, TTS·평가·GPU 전영역)

### 막힌 것 / 고민
- **삼성헬스 실데이터 = 폰 없이 끝까지 불가** — 라이브 자동연동의 마지막 단계(개발빌드 APK 설치·삼성로그인·권한"허용"탭·실센서)는 물리 폰+사람 손 필요. 어댑터·점수 로직은 다 됨(더미 검증). **빌드 없이 실결과 = 삼성헬스 "데이터 내보내기(export)" → 파서**(②경로, 발표 백업)
- 실연동은 별도·나중 PR(개발빌드+민경이+모세종). 이번 PR엔 **로직만**, 실연동은 핸드오프로

### 내일 할 일
- [ ] 삼성헬스 export 파일 받으면 파서 작성 → 본인 실데이터 회복점수(발표 실결과)
- [ ] (실연동 붙으면) RN+엔드포인트 별도 PR
- [ ] llama-server LLM 상시 서빙 구동

---

## 2026-06-10 (Day 10)

### 오늘의 목표
- 윤한님 백엔드 보조 중 발생한 `report.py` 머지 충돌 해결 (서브컴 `git pull` 막힘)
- 아침 로그 PetResponse 500 잡기

### 한 일
- [x] **`report.py` 머지 충돌 해결** — 윤한님 `backend/kyh`(PR #181 **이전** dev 기반)가 `play_count`/`session_count`를 추가해 둬, 내 ⑧ `access_counts`/`recovery_signal`와 충돌. 윤한님 브랜치는 안 건드리려 **내 jeonghwanju에서 `backend/kyh`를 끌어와** 충돌을 띄워 해결
  - **둘 다 살림(additive)**: `build_report`가 `access_counts`+`play_count`+`session_count` 셋 다 받게. `recovery_signal` 입력은 `access_counts`만(윤한님 확인: `play_count`는 누적 카운터라 '추세' 계산엔 못 씀 → 별도 표시 지표로만)
  - 감정키 `score`로 통일(`mood`는 폐기된 옛 키) · `services/report.py` 중복 `from bson import ObjectId` 제거
- [x] **통합테스트 페이크DB 보강** — 머지된 `get_report`가 신규로 `media_assets.find()`·`access_logs.count_documents()`를 호출 → `_FakeCollection`에 `count_documents` 추가 + 두 테스트에 `media_assets` 컬렉션 주입
- [x] **PetResponse 500 수정** — 윤한님 커밋 `cb6f3b1`이 `PetResponse`에 `bucket_list`/`caller_name`을 기본값 없이 추가 → 두 필드 없는 옛 DB 문서가 `PetResponse(**doc)`에서 ValidationError(GET /pets 500). 두 필드 기본값 `None`으로 (`schemas/pet.py`)
- [x] **검증** — 충돌 마커 0, 머지 완료, 테스트 **27개 통과**(backend 6 + ai/evaluation 21), ruff·black clean. 로컬 커밋 `6d68ec4`(부모 2개 = dev + 윤한 kyh)
- [x] **④ TTS E2E 음성 재생 막힘 진단 + nginx 수정 검증**(오후) — 경이님 TTS+영상 E2E가 안 되던 원인 추적. 체인(앱→`/api/v1/messages/tts`→Qwen3 GPU 터널→wav 저장→앱이 `audio_url`로 wav 받기) 중 **마지막 한 칸만 막힘**:
  - **원인 = NCP 리버스프록시(nginx) 라우팅**(내 코드 아님). 백엔드는 `main.py:49`에서 `/uploads` StaticFiles 마운트 정상인데, 공개 도메인이 `/uploads/*`를 **프론트 SPA 캐치올**로 보내 wav 대신 `index.html`(200·461B) 반환. e2e_smoke가 `audio_url`을 **다운로드 안 해서** 이 끊김을 못 잡음(API 201만 통과)
  - **윤한님이 nginx `/uploads/` 블록 추가·reload** → 재검증: 새 wav `200·149804B·audio/x-wav`+본문 `RIFF..WAVE`(진짜 wav) / 없는 파일 `404 json`(백엔드 StaticFiles 정상 응답). **체인 전체 통과, 음성 재생 가능 확인**
  - **남은 1건(경이님 프론트 영역)**: `message.jsx:241`이 `audio_url`을 host 없이 상대경로로 재생 uri에 씀 → `tts.jsx:58`처럼 `API_BASE` 프리픽스 필요(`tts.jsx` 화면은 이미 정상)

### 막힌 것 / 고민
- **서브컴 unmerged 잔존** — 그쪽 로컬 `report.py` 충돌은 내 작업과 별개라, dev 반영 후 그쪽에서 `git merge --abort` → `git pull` 해야 풀림
- **PR 여부 미결** — `jeonghwanju` 푸시는 했으나(내 브랜치라 dev 무영향), `dev`로 PR 올릴지는 환주 결정 대기. 윤한님 브랜치는 끝까지 무손상
- **TTS 재생 = 인프라 의존** — 음성 자체는 되는데 공개 경로(`/uploads`) 라우팅이 nginx에 달려 있어, 내 코드만으론 E2E 보장 불가. e2e_smoke가 파일 다운로드까지 안 해 **"API 통과=재생 가능"이 아님**(오늘 실제로 어긋났음)

### 내일 할 일
- [ ] PR 올릴지 결정 후 진행 (dev로, 리뷰 1명)
- [ ] 경이님께 `message.jsx` `audio_url`에 `API_BASE` 프리픽스 보정 전달 (프론트 영역, 내가 직접 수정 X)

---

## 2026-06-09 (Day 9)

### 오늘의 목표
- ④ TTS **remote 실연동**(NCP→GPU 왕복) 실제로 되는지 검증 — 세종 태스크
- LivePortrait driving 터널 점검

### 한 일
- [x] **TTS CLI `--gender man/woman` 수정** — `build_instruct`는 이미 성인 앵커를 처리하나 argparse `choices`가 boy/girl로 막혀 `--gender man` 실행 시 오류. choices에 man/woman 추가(WebUI와 동작 일치) → **PR #162 머지**
- [x] **TTS remote 실연동 검증 성공** — 앱 인증(JWT)→NCP `POST /api/v1/tts`→터널→Qwen3→wav 전체 체인 확인. 처음엔 **Google 폴백(mp3·1초)** 타다가, 세종 재배포(`TTS_SERVER_URL` 적용) 후 **Qwen3(wav·8.5초·`_girl_`)로 전환** 확인. ngrok 검사기에 `/tts/synthesize 200`(16:31) 도달 + 들리는 wav 확보. **mp3→wav·1초→8.5초가 폴백/실연동 구분 증거**
- [x] **`ai/GPU_SERVER.md §12` 문서화** — 실연동 검증 결과(백엔드 `_qwen3_remote` 계약 일치표) + NCP 인계 체크리스트(`TTS_SERVER_URL` 한 줄 + 테스트 호출) → **PR #164 머지**
- [x] **PR #163 리뷰·approve** — 민수 봉투 애니메이션 + BGM 연결. gitignore 예외·`require` 복구 정상 확인, BGM 9.6MB 바이너리는 비차단 FYI로 코멘트
- [x] **LivePortrait 영상 호스팅 = 정환주 GPU 아님 발견** — driving 테스트하려다 **LP 엔진이 정환주 GPU에 통째로 없음** 확인: LP_HOME(`c:\JMS\2_project\LivePortrait`) 없음 · d3.mp4 없음 · `liveportrait` conda env 없음 · 8001은 우리 wrapper 껍데기(실행 중 프로세스 env까지 확인=전부 미설정·local 모드). `/health`는 설정값만 echo라 **엔진 없어도 `0.5` 떠서 "가짜 성공"** 위험 → env만 세팅하면 발표 때 터짐
- [x] **영상 구조 결정 — 민수 3060 + 별도 터널** — 8GB에 영상+음성 동시 = VRAM 충돌(세종 우려)이라, 영상은 **엔진이 실재하는 민수 3060에서 자체 터널**로 분리. ngrok 무료 한도(월 1GB·고정도메인 1·최대 3엔드포인트) 기반 리스크 분석 정리: 민수 고정도메인 필수 / 두 PC = 장애점 2개 / NCP에 `LIVEPORTRAIT_REMOTE_URL` 추가 / 합치기는 NCP에서

### 막힌 것 / 고민
- **Qwen3 폴백 갭** — `TTS_SERVER_URL` 설정 상태에서 터널이 죽으면 `_qwen3_remote`에 try/except가 없어 **Google 자동폴백 안 되고 500**. 발표 안정성 위해 윤한께 폴백 보강 요청 예정
- **터널 보안** — `/tts/synthesize`·`/generate`가 인증 없는 공개 엔드포인트(PUBLIC 레포에 주소 노출) → 주소 알면 누구나 GPU 호출 가능. A안(프록시 `X-API-Key`) 제안만 했고 미적용
- **음성 방향** — Google 단독 회귀 검토했으나 비추(기계음 + 그간 Qwen3 작업 무위). **Qwen3 주력 + Google 폴백** 유지가 결론

### 내일 할 일
- [ ] 윤한: Qwen3 실패 시 Google 폴백 `try/except` 추가 요청
- [ ] 민수: 3060 고정도메인 터널 + NCP `LIVEPORTRAIT_REMOTE_URL` 추가 → NCP→민수 영상 왕복 테스트
- [ ] (선택) 보안 A안 — 프록시 `X-API-Key` 헤더 인증

---

## 2026-06-08 (Day 8)

### 오늘의 목표
- 내 파트 점검 → 남은 일 확정. ⑧ 리포트 코어는 완성인데 **`llm_logs` 실데이터가 안 흘러** `usage.total_calls=0`(껍데기) → 실데이터 연결
- 방향: **라이트 로깅**(횟수·종류·지연·성공만, 토큰 제외) — `build_report`가 `len`·`kind`만 쓰므로 토큰 없이도 리포트가 산다

### 한 일
- [x] **dev 동기화** — 로컬 `jeonghwanju`가 dev보다 20커밋 뒤(PR #119 메시지경로 통합·#123 문서 머지 반영). `message.py`가 `generate_message` 통합본임 확인
- [x] **`ai/evaluation/logs.py`에 async 헬퍼 `alog_llm_call` 추가** — 기존 `save_log`/`log_llm_call`은 동기(pymongo)뿐, 백엔드는 motor(async)라 못 씀. `resp=None`이면 토큰 0(라이트 로깅)에 그대로 맞음
- [x] **테스트** — `test_logs.py`에 async 헬퍼 테스트(`asyncio.run`, plugin 불필요) 추가 → `ai/evaluation` **11 passed**
- [x] **backend 호출부 연결**(모세종 영역, 사전 공유 필요) — `message.py create_message`·`mission.py`를 `try/finally`로 감싸 LLM 흐름 1건 로깅. ⚠️ 위기 경로 **early-return** 때문에 블록 뒤 로깅은 KIND_CRISIS를 놓침 → `try/finally`+분기별 `kind`로 해결. best-effort(`try/except`)
- [x] **통합 정합 시뮬** — 로깅→`pet_id` 필터→`build_report` 집계가 `total_calls`·`by_kind` 정확히 맞음 입증(실 DB 없이)
- [x] (별건) **GPU 터널 상시화** — ngrok 무료 고정도메인 확정(`rerun-devious-reaffirm.ngrok-free.dev`, `--url` 검증) + 로그온 시 서버·터널 자동기동 등록

### 막힌 것 / 고민
- **backend 전체 pytest 로컬 불가** — 내 PC(ai_env)에 motor 미설치(정환주=AI 환경). ruff·black·문법·ai-import + ai/evaluation 단위테스트로 갈음, **backend pytest는 CI 위임**
- **이중 writer** — 김윤한 `POST /api/v1/llm-logs`(#34) vs 내 서비스 직접 insert. 같은 컬렉션·스키마 정합(내 doc이 상위집합)이라 충돌은 없으나 **쓰기 경로 일원화 협의 필요**
- **커버리지 공백** — ② 감정 체크인 경로 위기 감지는 이번 미계측 → `by_kind.crisis`는 부분집계(후속)
- 메트릭 의미: "기능 호출 1건=1로그"(LLM API 호출 단위 아님), `latency_ms`=기능 end-to-end. UI/문서에 "LLM 호출 횟수"로 쓰지 말 것

### 내일 할 일
- [ ] 모세종께 backend 로깅 변경 공유 → PR(base dev, 모세종 리뷰)
- [ ] (후속) 토큰 정확 집계 — `provider.generate` 응답 메타 노출(반소람 협의)
- [ ] (후속) ② 감정 체크인 경로 로깅 연결

---

## 2026-06-08 (Day 8) — ④ TTS: 어린아이+감정 음색 탐색

> 같은 날 오후 별건. **"감정이 실린 진정성 있는 추모 목소리"** + 어린아이 톤 매칭.

### 오늘의 목표
- 그동안 다룬 TTS 엔진을 **라이선스 + 감정 입히는 법(튜닝)** 관점으로 한 곳에 통합 정리
- 어린아이+감정 톤을 **파인튜닝 없이 instruct(추론 시점 제어)**로 우선 뽑아 청취

### 한 일
- [x] **`ai/tts/TTS_라이선스_튜닝_조사.md` 신규** — 흩어진 엔진 노트를 라이선스×감정튜닝×우리실험으로 통합. 감정 입히는 **5방법**(①LLM 텍스트 리라이팅=엔진무관 공통레버 / ②prosody / ③스타일태그 / ④instruct 음색설계 / ⑤음성복제). **상업무료+감정 자연어제어** 후보 4종(**Qwen3-TTS·VoxCPM2·CosyVoice2·Chatterbox**), 라이선스 함정(F5·IndexTTS2 비상업 / Edge 개인만 / EL·Google·Azure·Typecast·CLOVA 유료) 정리
- [x] **파인튜닝 보류 결정** (조사문서 §5) — 어린아이+감정을 가중치 학습으로 고정하려 했으나 **시작 자체 불가**: GPU(공식 full SFT = 1.7B 20GB+ / 0.6B 12GB+ → RTX 5060 **8GB 불가**), 데이터(사람 녹음 30분+ 없음, **아동 음성 학습은 법·윤리 장벽**), LoRA는 비공식. → **instruct(학습 0)로 우선 진행**, 24GB+ GPU·음성데이터 확보 시 재개
- [x] **`ai/tts/qwen3_emotion.py` 신규** — 음색만 주던 이전 design이 "별로" → instruct에 **나이(어린아이)+감정 동시 주입**. 반복 탐색에서 `soft/warm/breathy/loving/soothing` 누적이 **과한 느끼함+속삭임(숨소리 친밀톤)** 유발 확인 → **낭독·투사 레지스터**(matter-of-fact / reading aloud / projected at normal distance / not whispering, 감정어 0)로 전환. boy_read·girl_read v9~v12(seed·temp 변주). + **사용자 다이얼**(warmth·bright·pace + temp·seed 노브 직접 합성, 아이 앵커 고정으로 아저씨톤 방지). **boy_warm(원본 텍스트, 과한 호흡 제거) 베스트 판정**
- [x] **`ai/tts/tone_down.py` 신규** — '쩅한'(날카로운 고역) 후처리 EQ. RBJ biquad **하이쉘프 컷** + **노치 스윕**(3~7kHz 대역별로 깔아 쨍함 fc를 청취로 짚기) + 협대역 노치. 원본 안 건드리고 새 파일로만(가역). `_output/qwen3_emotion/eq/` 산출
- [x] **`ai/tts/voxcpm_design.py` 수정** — 밝고 발랄한 톤 제외, **추모 낭독용 차분한 어린이 톤 6종**(calm·whisper·warm × girl·boy)으로 교체

### 막힌 것 / 고민
- **모델 성향 함정** — child+gentle instruct는 숨소리·느끼함으로 기우는 경향이 강해 감정어를 넣을수록 속삭임이 도드라짐. 낭독 레지스터로 우회했으나, **instruct만으로 "감정 실린 진정성"엔 한계** 가능성 → ①(LLM 텍스트 리라이팅) 병행 검토
- **상업무료 후보 한국어 톤 미청취** — VoxCPM2·CosyVoice2·Chatterbox는 라이선스만 통과, **실제 한국어 톤 품질은 들어봐야** 결론
- 파인튜닝은 자원·데이터·법윤리벽으로 보류 (위 §5)

### 내일 할 일
- [ ] VoxCPM2 차분 어린이 6종 청취 + Qwen3 `boy_warm`·다이얼 결과와 비교
- [ ] 상업무료 후보(Qwen3·VoxCPM2·CosyVoice2·Chatterbox) 한국어 톤 **블라인드 A/B**(EL 라운드2 방식)
- [ ] 베스트 톤 확정 시 EQ 후처리 fc·dB 값 고정

---

## 2026-06-07 (Day 7)

### 오늘의 목표
- 세종님 피드백 반영: **"TTS는 사람이 직접 들어야 의미 있다"** → EL 한국어 실청취로 Day6 결론 재검증
- Day6 "Google 유지"는 자동지표(CER·Gemini)로 낸 결론 → 사람 A/B로 다시 판정

### 한 일
- [x] **사람 블라인드 A/B 하네스 신설(`ai/tts/pairwise_listen.py`·`blind_listen.py`)** — 각 후보를 현행 Google warm(앵커)과 1:1, 좌우 seed 셔플. 자동지표(CER=감정 못 잼 / Gemini=이해상충)는 **결정 근거에서 폐기**, 참고용만
- [x] **EL 한국어 보이스 9종 웹 수동 추출** — Free API는 한국어 네이티브 차단 → elevenlabs.io 웹 Text to Speech(model `Eleven Multilingual v2`)에서 직접 듣고 9종 다운로드(`bomisori·annie·esther·jason·annakim·juan·leeho·mrk·onyu`)
- [x] **라운드2 A/B 청취 → EL 9전 9승(블라인드)** — 9페어 전부 EL 선택. "Google 너무 기계 / EL 사람 같음" → **원래 문제(기계음) 해소.** 라운드1(영어권 핸디캡, Google 6승) 완전 역전 → 핸디캡 탓 입증. **Day6 "Google 유지" 결론 뒤집힘**
- [x] **결론 2부 문서화** — ① 품질=EL 압승(해결) ② 비용(상업=유료)=PM 결정. 범위=폴백 `voiced_url` 한정(주력 PERSO 아바타+내장TTS 무관). `ENGINE_NOTES.md`§2·3·5·`AZURE_TTS_조사.md`·`SAMPLE_생성_가이드.md` 갱신

### 막힌 것 / 고민
- EL 상업=유료(Starter $5/월~) — CLOVA를 비교에서 뺀 그 "유료 기본료" 벽이 EL에도 적용. 품질만으로 "EL 채택 확정" 단정 금지(세종님 지적의 거울상) → **최종 채택은 비용 포함 PM 판단**
- 청취 n=1(블라인드 9/9라 강한 신호) — 팀이 원하면 2차 청취자 1명 더(선택, 비차단)

### 내일 할 일
- [ ] PM(모세종)께 라운드2 결과 공유 → 비용 포함 폴백 엔진 채택 결정
- [ ] 채택 시 `voiced_url` 폴백 엔진 교체(흠 적은 보이스 `bomisori·annie·esther·jason` 우선)

## 2026-06-06 (Day 6)

### 오늘의 목표
- PR #115(TTS Google→ElevenLabs 지정) 대응 — **저위험 평가 스파이크**로 전환 가치 실측
- ElevenLabs vs Google 객관 비교 → 전환 여부 결론

### 한 일
- [x] **ElevenLabs 비교 스파이크(`ai/tts/compare_elevenlabs.py`)** — 동일 보호자 위로 멘트를 ElevenLabs 4성격 + Google 4톤으로 합성해 A/B 샘플 생성. `synthesize()` 본체는 안 건드림(전환 보류)
- [x] **Free 플랜 한국어 제약 실측** — Free 키는 API로 한국어 네이티브 보이스 사용 불가(402/400) → 영어권 보이스만 → **한국어 평가 confound** 확인, `ENGINE_NOTES.md §5` 기록
- [x] **STT 발음 분석(`ai/tts/analyze_stt.py`)** — faster-whisper large-v3로 8개 mp3 받아쓰기 → CER. **8개 전부 0.0%**(발음으론 변별 불가). GPU(RTX 5060/sm_120)는 ct2 inference hang → CPU 사용(CER은 device 무관)
- [x] **Gemini 오디오 자동평가(`ai/tts/eval_gemini_audio.py`)** — STT로 못 재는 자연스러움·감정을 Gemini 2.5 flash 멀티모달 청취로 평가(참고용). Google 4톤 중 **warm 5/5 최고**, calm 감정 3/5. confound 있는 ElevenLabs는 제외
- [x] **결론: Google 유지 확정** — CER 변별불가 + Free로 ElevenLabs 공정평가 불가 → 전환 근거 없음. `ENGINE_NOTES.md` §3 추천 모순(주력 ElevenLabs)도 Google 유지로 정합

### 막힌 것 / 고민
- RTX 5060(Blackwell sm_120)에서 ctranslate2 4.7.2가 GPU inference hang(모델 로드는 성공) — ct2 재빌드 전엔 STT는 CPU로. 추후 GPU STT 필요 시 재검토
- ElevenLabs 진짜 한국어 평가는 유료 Creator+ + 네이티브 보이스 재샘플 필요(현재는 보류)

### 내일 할 일
- [ ] (전환 보류 유지) 유료 전환 판단 시 한국어 네이티브 보이스로 재샘플 평가

## 2026-06-05 (Day 5)

### 오늘의 목표
- AI 파트 **RAG 검색 파이프라인** 신설 + 검색 품질 실증 도구(Hit@1/MRR) 마련
- ④ TTS 음성 **다양화·실패 폴백** 안정화, 추모 낭독 방향 정리(PERSO 아바타)
- HHHHHMM 삶의 질 척도 모듈(선택·도먼트) 추가

### 한 일
- [x] **RAG 검색 파이프라인 신설(`ai/rag/`)** — ChromaDB 기반 임베딩·적재·검색 모듈(`config·embeddings·ingest·retrieve·store`) + 샘플 corpus + 테스트 → PR #79. `.env.example`·`.gitignore`에 RAG 설정/벡터 볼륨 추가
- [x] **RAG 검색 품질 실증 도구(`ai/rag/eval/`)** — 패러프레이즈 쿼리셋(`eval_queries.json`)으로 Hit@1/MRR 측정. 12개 전부 rank 1(Hit@1 100%·MRR 1.0)로 의미검색 정상 동작 확인 → PR #79
- [x] **RAG 벡터DB 스터디·비교 문서** — `ai/llm/VECTOR_DB_심화_스터디.md`(262줄) + `VECTOR_DB_비교.md`(138줄). 후보 비교·ChromaDB 선택 근거(PR #73·#79)
- [x] **④ TTS 음성 다양화** — soft 톤 추가 + 목소리 선택 기능 + `ENGINE_NOTES.md` 엔진 비교 노트(PR #71)
- [x] **④ TTS 합성 실패 폴백** — 합성 실패 시 샘플 mp3로 graceful 폴백 + `make_samples.py` + 테스트(PR #73)
- [x] **HHHHHMM 삶의 질 척도 모듈(`ai/hhhhhmm/`)** — `qol.py` + 테스트 + 폴더 가이드. 선택·도먼트(현재 비활성, 추후 연결용)(PR #73)
- [x] **PERSO 아바타 핸드오프 문서(`ai/tts/AVATAR_HANDOFF.md`)** — 성격→아바타 매핑 초안 + 청취 평가표(감정품질 검수용). 실호출·voiceId 확정은 장민수 API 테스트 대기
- [x] **1단계 확장 구조 역할 분담 문서** — 확장구조 단계별 AI 파트 책임 경계 정리
- [x] **⑧ build_report 핸드오프 재확인** — `ai/evaluation/HANDOFF_build_report.md` 필드 매핑 갱신

### 막힌 것 / 고민
- **TTS 방향 전환(팀 합의)** — PERSO **단독 TTS는 없음**(영상 더빙 엔드포인트만) 확정. 추모 낭독은 **PERSO AI Human 아바타가 읽는 MP4**가 주력, **Google TTS는 안정성 폴백**으로 이중 트랙. soft 톤 pitch 미세조정 워크플로는 폐기. 아바타 실호출은 장민수 영역, 나는 매핑·핸드오프·폴백 유지만.

### 내일 할 일
- [ ] 장민수 PERSO 아바타 API 테스트 결과로 성격→아바타 매핑·voiceId 확정 (`AVATAR_HANDOFF.md` 초안 → 확정)
- [ ] `services/tts.py` 인라인 `sys.path` → `app.core.ai_path` 정리 (Day 4 이월)

---

## 2026-06-04 (Day 4)

### 오늘의 목표
- AI 파트 외부 연동 실연결 — Gemini·Google Cloud TTS 키 발급 후 실제 동작 확인

### 한 일
- [x] **① 개발용 LLM(Gemini) 실연결 완료** — Google AI Studio 키 발급 → `.env` `LLM_API_KEY` 설정 → `python ai/llm/smoke_gemini.py` 정상 응답("네, 잘 연결되었습니다.") 확인
- [x] **④ TTS 실동작 확인 완료** — Google Cloud "Text-to-Speech API" 사용설정 + 서비스 계정 키(JSON) 발급 → `GOOGLE_APPLICATION_CREDENTIALS` 연결 → `synthesize()` 로 실제 mp3 합성 성공(약 3.6초, 한국어 Neural2-A)
- [x] `google-cloud-texttospeech` 설치(`ai/requirements.txt` 기준) 및 임포트 검증
- [x] **`.env.example` TTS 섹션 정합** — 코드가 실제로 읽는 값으로 수정(`TTS_PROVIDER`/`TTS_API_KEY` → `GOOGLE_APPLICATION_CREDENTIALS`·`TTS_VOICE`)
- [x] **`.gitignore` 비밀키 차단** — `ai/tts/*.json`·`gcp-tts-key.json` 추가(서비스 계정 키 커밋 사고 방지)
- [x] PR: #32(④⑧ 테스트 골격+`ai/requirements.txt`+TTS 셋업 문서) 머지됨, **#35(TTS 환경변수 정합 + 키 gitignore) 생성**
- [x] `jeonghwanju` 브랜치를 최신 `dev`로 머지(충돌 `.gitignore` 1건 해결) → PR diff 정리, **#35 머지됨**
- [x] **④ 백엔드 TTS 실연결 완료** — `backend/app/services/tts.py` stub → `ai/tts.synthesize()` 실제 호출로 교체. 합성 mp3를 `uploads/tts/`에 저장하고 `/uploads/tts/{file}` URL 반환. 인증 배선(backend cwd에서 루트 `.env` 로드 + 키 경로 절대화 + `sys.path`에 레포 루트 추가) + 블로킹 호출은 `asyncio.to_thread`. backend에서 실제 mp3 생성 검증 + `requirements.txt`에 `google-cloud-texttospeech==2.36.0` 추가 → **PR #40 머지**. (④ TTS 완성: 키→합성→백엔드, 화면=장민수 대기)
- [x] **팀 PR 리뷰 3건** — #33 로그인(RDB): `*.db` gitignore 누락(비번 해시 DB 커밋 위험) 발견 → 변경요청, 수정 후 승인 / #36: `tokens_used`↔`total_tokens` 필드 불일치(admin/usage 집계 샘) 발견 → 정합 확인 후 승인 / #38 위기감지 `assess_crisis` 실연동: worktree로 실제 동작(risk 0/2/3·1393 정상, 일반 그리움 오탐 없음) 검증 후 승인
- [x] **추모 윤리 경계 정리(팀 논의용)** — (1) "작별 편지"는 보호자가 직접 쓰게 돕고 AI는 **보호자 시점**으로 다듬기(반려동물 1인칭 ❌) (2) 추모 영상은 **입 싱크 ❌ · 살랑살랑 ambient만**(말하는 것처럼 보이면 1인칭의 시각版). 음성·영상 분리. 팀+강사 합의 필요.
- [x] **PROGRESS.md 본인 칸 갱신** — ④ TTS·⑧ 평가 ⬜→✅ (6/1에서 멈춰 있던 거짓 표시 정정, 본인 완료분만 반영) → **PR #58**
- [x] **⑧ build_report 백엔드 연결 핸드오프 작성** — `ai/evaluation/HANDOFF_build_report.md`: 필드 매핑(`score→mood`·`completed→done`) + `messages.count`→`llm_logs` 교체안 정리 → 모세종이 그대로 붙일 수 있게 (PR #58)

### 막힌 것 / 고민
- 처음 PR 올릴 때 `dev` 최신화를 안 해서 브랜치가 6커밋 뒤처짐 → PR diff에 이미 dev에 있는 파일이 끼어 보임. `git merge dev`로 최신화해 해결. **다음부터 작업 전 항상 dev 머지 먼저.**
- PR 승인 후 새 커밋이 올라오면 **승인이 자동 무효화(DISMISSED)** 됨 → "더 안 건드릴 때" 마지막에 승인받고 바로 머지하는 게 깔끔.
- backend는 `backend/.env`를 찾는데 그 파일이 없어 루트 `.env` 미로드 + 키 경로가 상대라 cwd에서 깨짐 → 서비스 코드에서 명시 로드·절대화로 해결(advisor 지적).
- Google Cloud TTS 결제 등록 필요(무료 한도 월 100만 자 내라 데모는 사실상 0원) — 추후 예산 알림 설정 검토

### 다음 할 일
- [ ] #38·#40 둘 다 머지됨 → 내 `services/tts.py` 인라인 `sys.path` → `app.core.ai_path` 사용하도록 후속 정리(작은 PR)
- [ ] ⑧ `save_log` 훅을 LLM 호출부에 연결(반소람) + `admin/usage`·report 컬렉션 합의(김윤한)
- [ ] 장민수 오디오 플레이어에 `/api/v1/tts` 연결 지원(응답 `audio_url` 사용법 공유)
- [ ] TTS 톤 값 ↔ 메시지 톤(③) 매핑 반소람과 합의
- [ ] 추모 편지/영상 윤리 팀+강사 합의
- [ ] Gemini 무료 티어 데이터 정책 확인(위기감지 민감정보)

---

## 2026-06-02 (Day 2)

### 오늘의 목표
- LLM 추론 서버 가동 (반소람 언블락) — AI 파트 환경 셋업 착수

### 한 일
- [x] AI 파트 작업 문서 8종 작성 (CLAUDE.md·TODO.md·ROLES.md·GPU_SERVER.md 등) → PR #3 dev 머지
- [x] GPU 환경 점검: RTX 5060 **8GB**, 드라이버 595.97, CUDA 13.2
- [x] 한국어 LLM 모델 조사 (EXAONE-3.5, Qwen2.5 후보)
- [x] 엔진·모델 결정: **Ollama + EXAONE-3.5-2.4B** (8GB 여유·한국어 양호)
- [x] EXAONE-3.5-2.4B 다운로드 (~1.7GB)
- [x] 위로 메시지·위기감지 테스트 (위로 3 + 위기 4 샘플) → 2.4B vs 7.8B 비교
- [x] **EXAONE-3.5-7.8B 채택** — 2.4B는 다중 샘플서 반려동물 1인칭 위반(2/3) 발견 → `ai/llm/MODEL_NOTES.md` 기록
- [x] `.env.example`·`SETUP.md §2-1` LLM 설정 채움
- [x] 추가 모델 비교(qwen3:4b·gemma3:4b·EXAONE-4.0) → 전부 탈락(위기 미탐/1인칭 위반/미지원), **EXAONE-7.8B 견고히 확정**
- [x] **결정 변경: 개발용 LLM 로컬 EXAONE → Gemini API** (강사 권고 + 8GB 제약). EXAONE은 로컬 폴백으로 유지. `.env.example`·`SETUP.md`·`MODEL_NOTES.md` 정정 + `smoke_gemini.py` 추가
- [x] **서비스 시나리오 조사·문서화** — 3단계(아플 때→이별 준비→떠난 후) 확정안 정리. 펫로스 회복 단계·HHHHHMM 척도·병원 연계·앱 리텐션 근거 조사 → `docs/SERVICE_SCENARIO.md` (⚠️ "반려동물 시점 편지"는 1인칭 금지선 위반 → 보호자 시점으로 명시)
- [x] **④ TTS 골격 작성** — `synthesize(text, tone)` + Google Cloud TTS 연동 스텁, `TtsTone` 톤 매핑, 긴 텍스트 분할 (보호자 대상 낭독)
- [x] **⑧ 평가 골격 작성** — `LLMLog` 스키마 + `save_log`(원문 PII 미저장) + `build_report`(순수 함수) → `ai/evaluation/`
- [x] **⑧ `llm_logs` 스키마 정합** — `LLMLog.to_doc()`에 `total_tokens` 추가(백엔드 `/admin/usage` 합산과 정합) + `admin.py` 예시에서 `prompt`(원문) 제거(PII). *버그 아님, 통합 전 계약 맞춤*
- [x] **④⑧ 골격 테스트 10개 작성·통과** + `ai/requirements.txt` 작성 + `SETUP §2-3` TTS(Google Cloud) 셋업 채움 — 키 발급 전까지 솔로 가능분 처리
- [x] `ai/TODO.md` ④⑧ 현황 반영, `.gitignore` 음성 산출물 정리
- [x] PR #9(장민수 LivePortrait 템플릿) 리뷰 / 스크럼 강사님 질문 정리(PR #15→#16, 라이선스·위기감지만)
- [x] PR: #17(Gemini 전환+시나리오) 머지, **#19(TTS·평가 골격) 생성**

### 막힌 것 / 고민
- Gemini 전환으로 EXAONE 라이선스·8GB 속도 문제 해소. 단 **Gemini도 1인칭 회피·위기 미탐 0 골든셋 통과 검증 필요**(반소람과)
- 위기감지(⑦) 입력이 외부 API로 나감 → Gemini 무료 티어 데이터 정책 확인 필요(민감정보)

### 다음 할 일
- [ ] Gemini API 키 발급 + `smoke_gemini.py` 연결 확인 → 반소람 언블락
- [ ] ④ TTS: Google Cloud 인증·키 설정 → 실제 음성 1회 합성, 음성/톤 품질 점검
- [ ] `POST /api/v1/tts` 스텁(김윤한과) → 장민수 오디오 플레이어 언블락
- [ ] ⑧ `save_log` 훅을 LLM 호출부에 연결(반소람) + `admin/usage`·report 컬렉션 합의(김윤한)
- [ ] 톤 매핑 테이블 반소람 ③ 톤과 값 합의
- [ ] 팀 확인: 보호자 시점 편지 합의 / 마감 6/17 vs 6/19
- [ ] PR #16·#19 머지 처리
