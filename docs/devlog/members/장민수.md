# 개발일지 — 장민수 (멀티모달)

> 최신 날짜를 **맨 위**에 추가하세요. 통합 일지: [../README.md](../README.md)
>
> <details><summary>📋 작성 템플릿 (복사해서 사용)</summary>
>
> ```markdown
> ## YYYY-MM-DD (Day N)
> ### 오늘의 목표
> -
> ### 한 일
> - [x]
> ### 막힌 것 / 고민
> -
> ### 내일 할 일
> - [ ]
> ```
> </details>

---

## 2026-06-18 (Day 18)

### 오늘의 목표
- 강사님 파이프라인(MuseTalk v1.5 → LP animals)으로 추모영상 립싱크 재현
- TTS 음성 Gemini 전환 (강사님 권고)

### 한 일
- [x] Gemini TTS 도입 — `_gemini_tts.py` 작성(API 키 환경변수, 자연어 스타일 지시), 편지 전체 음성 생성(`letter_gemini.wav`, 87초·24kHz)
- [x] MuseTalk v1.5 → LP animals 재현 — therapist 드라이버 + Gemini 음성, region=all·mult 1.5·no-stitching, 29fps mux (`2차프로젝트5팀레인보우브릿지.mp4`)
- [x] 립싱크 desync 트러블슈팅 — 원인 후보 전수 배제: GPU·모델(unet/vae/whisper)·음량·fps(29.97→29 정수화)·30초 whisper 세그먼트·드라이버 해상도(720p→Full HD)
- [x] 검증 방법 교정 — RMS 에너지·대비는 립싱크 지표로 부적합 확인 → 강아지 입–발화활동 교차상관 + 대조군(강사님 영상) 비교로 전환. 강사님 정상 영상 corr +0.41(lag~0) vs 우리 출력 +0.02 → desync 정량 확인
- [x] 핵심 진단 — 로컬 MuseTalk이 Gemini TTS 립싱크를 입에 반영 못함(출력이 드라이버 영상 모션으로 회귀). 강사님 82초 풀버전은 동일 Gemini(24kHz)·29fps로 정상. whisper 음성 특징은 정상(음성 구분 O)
- [x] LP keypoint 우회책 채택 — `wav_lipsync.py`로 오디오 RMS → 입 개폐 keypoint 직접 매핑(`maltese_wav2kp_final.mp4`, 87초). 입–에너지 corr ~0.84, lag +34ms
- [x] 발표 자료 정리 — 팀 회고서 "남은 과제"·자기 평가서·기술 회고 슬라이드(시도1 설명, 23↔30번 모순) 수정, AI휴먼 정량평가 리포트 검토

### 막힌 것 / 고민
- 로컬 MuseTalk TTS 립싱크 미반영 근본 원인 미규명 — 동일 입력으로 강사님 기기는 정상, 환경/버전 차이 의심. 강사님 실행 명령 수령 불가
- LP keypoint 우회책은 에너지 기반 개폐라 음소 단위 정밀 립싱크 아님 (corr은 입–RMS 직접 매핑이라 구조적으로 높게 나옴)
- 검증 지표(RMS·대비)를 초반에 여러 번 잘못 신뢰 → 대조군 비교로 뒤늦게 교정

### 내일 할 일
- [ ] (여유 시) MuseTalk 정식 립싱크 복원 재시도 — 강사님 환경 차이 확인
- [ ] wav_lipsync 입 벌림(MOUTH_SCALE) 튜닝으로 가시성 개선

---

## 2026-06-17 (Day 17)

### 오늘의 목표
- wav_lipsync.py 자동 kp 탐지 완성 (다른 동물 사진 대응)
- 강사님 파이프라인 파악 및 동일 방식 재현

### 한 일
- [x] 새 말티즈 사진(maltese_new2.jpg)으로 source 교체 — kp 자동 탐지: 입 kp[19,18], 눈L kp[8,11,12], 눈R kp[4,5,14]
- [x] wav_lipsync.py v6 완성 — `auto_detect_kp()` 추가, 코·귀 움직임 제거, 고개 amplitude 축소
- [x] 강사님 추모영상(하늘이_추모영상_짧은버전.mp4) 수령·분석 — 512×512, 29fps, 15초, LP animals 방식 확인
- [x] 강사님 파이프라인 문서 수령 — MuseTalk v1.5 → LP animals 4단계 구조 파악
  - `therapist_talking.mp4` + wav → MuseTalk v1.5 립싱크 → LP animals (region=all, multiplier=1.5, no-stitching) → ffmpeg mux
- [x] mixkit 치료사 영상 다운로드 (therapist_talking.mp4)
- [x] LP animals + talking.pkl 테스트 완료
- [x] man_letter_1st.mp4 → LP animals (region=all, multiplier=1.5, no-stitching) 시도

### 막힌 것 / 고민
- LP animals에 사람 영상 직접 입력 시 모션이 전달 안 됨 — MuseTalk로 립싱크 먼저 생성 후 LP animals에 입력하는 순서를 지키지 않아서 발생

### 내일 할 일
- [ ] therapist_talking.mp4 + letter_1st.wav → MuseTalk v1.5 립싱크 생성
- [ ] MuseTalk 결과 → LP animals (region=all, multiplier=1.5, no-stitching) 적용
- [ ] 발표 영상 파일명 `2차프로젝트5팀레인보우브릿지.mp4`로 확정
- [ ] wav_lipsync.py `generate_lipsync()` 함수화 후 소람님 인계

---

## 2026-06-16 (Day 16)

### 오늘의 목표
- 1인칭 편지 LP 생성 오류 확인 + dev 최신화
- 발표용 시연 영상 촬영·편집 (3~5분, 민경이와 동시 진행)
- 정량평가(SyncNet) 요청 대응

### 한 일
- [x] jangminsu 브랜치 변경사항 점검 후 push
- [x] 1인칭 편지 LP 생성 실패 신고 진단 — `LIVEPORTRAIT_API_URL`(코드) vs `LIVEPORTRAIT_REMOTE_URL`(팀 컨벤션) 변수명 불일치 발견 → dev에 이미 수정 반영된 것(커밋 `ef18b3a`·`4b76a54`) 확인
- [x] dev 최신화 — 로컬 `dev` ref가 stale해서 "Already up to date"로 오판되던 문제를 `origin/dev` 직접 머지로 해결, fast-forward 병합 완료
  - 병합 내역: server.py 로깅 미들웨어 추가, memorial.py 1인칭 가드레일 오탐 수정, config.py/media.py `LIVEPORTRAIT_REMOTE_URL` 변수명 통일, message.jsx·login.jsx 펫 사진 폴백 표시 추가
- [x] 시연영상_시나리오.md 검토 — 7막 구성 중 멀티모달 관련 장면(GIF/LP 결과, 1인칭 선물함, TTS 낭독) 파악
- [x] 발표 영상 제작용 소스 자료 정리 — 실제 데모 펫(하늘이) 사진 20장(`backend/scripts/image/`), BGM(`frontend-rn/assets/audio/`), LP driving 영상 7종(`ai/liveportrait/driving/`), 브랜딩 이미지 위치 확인
- [x] PR 현황 확인 — 오픈 PR은 #336(민경이, approved, 머지 대기) 외 없음
- [x] LivePortrait driving 최종 확정값 재조사 — 중간 실험 단계였던 d11/d12·EdMarkey0와 구분해, 실제 최종 채택은 `d3.mp4` + `driving_multiplier 0.5`(2026-06-09 확정 레시피, PR #188)임을 확인
- [x] `pipeline.py`의 `DEFAULT_DRIVING` fallback 경로가 주석(d3)과 다르게 `d0.mp4`로 남아있는 불일치 발견 (PR #188에서 multiplier만 수정되고 경로는 미수정)
- [x] 반소람님 SyncNet 정량평가 요청 확인 — 환경에 dlib·빌드도구(cmake/VS Build Tools) 없어 셋업 별도 필요함을 회신

### 막힌 것 / 고민
- SyncNet 평가 셋업: dlib 빌드도구 부재로 즉시 착수 어려움 — LP 서버 복구 후 착수 예정
- `pipeline.py` DEFAULT_DRIVING fallback(d0→d3) 수정 여부 미결정

### 내일 할 일
- [ ] 영상 백업본 준비 (모세종과 공동, 6/17)
- [ ] `pipeline.py` DEFAULT_DRIVING fallback d3.mp4로 정정 여부 결정
- [ ] SyncNet 셋업 착수 (LP 서버 복구 후)

---

## 2026-06-12 (Day 12)

### 오늘의 목표
- 온보딩 화면 버그 수정 및 디자인 개선
- 사진 자동 선택 → 추모영상 파이프라인 구조 파악 및 팀 공유

### 한 일
- [x] **온보딩 진입 라우팅 수정** (PR #246) — `app/index.jsx`가 `/(auth)/login`으로 직행해 온보딩이 아예 안 보이던 버그 수정 → `/(auth)/onboard`로 변경
- [x] **온보딩 하단 잘림 수정** (PR #246) — `paddingBottom: 36` 고정값 → `useSafeAreaInsets`로 기기 홈바 자동 대응
- [x] **온보딩 시트 디자인 개선** (PR #249) — 흰 시트 투명도 0.96→0.74, 4면 둥근 모서리 + 여백으로 floating 카드 형태, 연보라 테두리 추가, "이미 계정이 있어요" 버튼 가시성 개선
- [x] **온보딩 동의 체크박스 추가** (PR #254) — 강사님 윤리 가이드 요건 반영
  - "앱 사용 기록을 감정 회복 분석에 활용합니다" 동의 문구 + 체크박스
  - 동의 전 시작하기 버튼 비활성화, 동의 후 활성화
- [x] **사진 자동 선택 → 추모영상 파이프라인 전체 구조 분석** 및 팀 공유
  - `photo_selector.py`(Gemini Vision 정면 판별 + Laplacian 선명도·밝기·해상도·종횡비·MediaPipe 얼굴 크기 채점) → `select_best_pet_photo` → `run_liveportrait` 흐름 확인
  - 백엔드·AI 구현 완료, 프론트 연결 필요 → 민경이님 PR #248로 완료 확인
  - `photos.jsx`에서 `usage: 'liveportrait'`로 업로드 → `media_assets` 저장 → 자동 선택 가능한 구조 파악
- [x] dev 최신화 수차례 (PR #244~#254 반영) — TTS 무음 버그, LP GIF 비동기 job, Docker healthcheck 등 수신
- [x] ngrok → Cloudflare Tunnel 전환 내용 파악 및 팀 공유 업데이트

### 막힌 것 / 고민
- LP GIF 비동기 job 방식(PR #250)이 반영됐는데 백엔드 `_generate_remote_gif` 폴링 로직 변경도 같이 들어왔는지 직접 E2E 테스트 필요
- iOS TestFlight 계정 확보됐으나 EAS Build 설정은 민경이님 진행 중 → 실기기 테스트 대기

### 내일 할 일
- [ ] 발표 데모 리허설 참여
- [ ] LP 샘플 영상 pre-render (GPU 서버 Cloudflare 터널 안정화 후)
- [ ] E2E 통합 테스트 — 사진 업로드 → 자동 선택 → 영상 생성 전 구간

---

## 2026-06-11 (Day 11)

### 오늘의 목표
- 프론트 UI 리디자인 마무리 + dev 머지
- LivePortrait 좀비 프로세스 문제 수정

### 한 일
- [x] **온보딩 화면(onboard.jsx) 신규 제작** — 파스텔 일러스트 배경(onboard_bg.png), 별 반짝임 14개, 하단 시트, 회원가입/로그인 진입
- [x] **편지 해금 화면(message.jsx) ✉️ 리디자인** — 민경이님 PR #229 최신본 위에 병합
  - 다크 네이비 배경, 16개 별 반짝임(MsgStar), ✉️ 위아래±3px 살랑살랑 흔들림
  - 편지 단계 제목 헤더, Easing.out(cubic) 자연스러운 봉투 열림
- [x] **PR #231 dev 머지 완료** — onboard + message.jsx ✉️ UI
- [x] **LivePortrait 좀비 프로세스 수정** (소람님 리포트 반영)
  - `conda run` 래퍼 제거 → `python.exe` 직접 호출 (`LP_PYTHON`)
  - `threading.Semaphore`로 동시 추론 1개 제한 (`MAX_CONCURRENT`)
  - `Popen + communicate(timeout) + proc.wait()` — 타임아웃 시 고아 방지
  - 소람님 서버 적용·검증 완료: 좀비 0개, RAM 34% (기존 92%)

### 막힌 것 / 고민
- dev sync 시 `git checkout origin/dev -- docs` 로 다른 팀원 개발일지까지 커밋에 포함시킨 실수 → 앞으로 파일 단위로 콕 집어서 받기
- onnxruntime CPU 폴백 문제(warm 75초): cuDNN 교체하면 30초대 가능하지만 발표 직전 리스크 → 발표 후 진행

### 내일 할 일
- [ ] 소람님 `git merge dev`로 pipeline.py 최신화 확인
- [ ] 발표 데모 리허설 참여

---

## 2026-06-10 (Day 10)

### 오늘의 목표
- 발표 중간점검 대응 (체크리스트·갭분석 멀티모달 검증)
- 영상 재생 데이터(능동 시청) 기능 마무리

### 한 일
- [x] **driving_multiplier 0.4→0.5** (PR #188) — 발표 확정값. 코드 기본값이 0.4라 소람 PC env 누락 시 사고 위험 → 기본값 0.5로 안전망
- [x] **중간진도점검 체크리스트·기능완성도 갭분석 멀티모달 검증·정정**
  - corpus 75→63건, **comfort·vet_protocol 카테고리 빔** 발견 → 반소람 확인 요청
  - GPU remote: "정환주 터널 진행중" → **소람 PC 이전·2터널 실증 완료**로 정정
  - TTS wav→영상 버그: `merge_audio`로 **.wav 음성 합쳐지는 것 실측 검증**
- [x] **강사 회의록 검토** — 능동 시청 "버튼 클릭 = play_count" 확정이 **우리 구현과 일치** 확인 (강사: "한 번 보면 끝까지 볼 컨텐츠" → 시청 비율 X)
- [x] **영상 능동 시청 트리거** (recordPlay + asset_id) 작업 → **민경이 #198에서 이미 구현(①②③)** 확인 → 중복 PR #200 닫고 dev 정리
- [x] **photo_selector 분석** — 입 다뭄 미판정 발견(정면이면 입 벌려도 통과 → LP 혀 늘어짐 위험), 팀 논의 대기

### 막힌 것 / 고민
- ⚠️ **풀 미반영으로 중복 작업** (recordPlay) — 민경이가 이미 머지한 걸 모르고 작업. **작업 전 fetch+dev 동기화 습관화** 필요 (반성)
- photo_selector 입 다뭄 판정 추가 = 모세종 #193 기여분이라 **팀 논의 후** 진행
- 영상 데모 작동 = 소람 PC LP 터널 + NCP `.env` 발표 직전 점등에 의존

### 내일 할 일
- [ ] 영상 데모 E2E — 소람 LP 터널 + `.env` 반영 + 버튼 play_count 실작동 확인
- [ ] photo_selector 입 다뭄 판정 (팀 논의 결과 따라 결정)
- [ ] 발표 시연 리허설 + 사전녹화 백업본 점검

---

## 2026-06-09 (Day 9)

### 오늘의 목표
- 강사 스크럼 피드백 대응 (펫로스 극복 "효과 입증" 근거 마련)
- 발표 데모 자동화 마무리 + 안전판 확보

### 한 일
- [x] **펫로스 회복 논문근거 보강** (PR #165) — 발표용 정량 근거
  - 검증 척도 **ICG**(α=0.93, >25점 기능 손상)·PBQ, **예기 비탄**(시한부 진입점의 학술 명명)
  - 효과크기: 표현적 글쓰기 **g=0.388** / 심리개입 31 RCT **g=0.41·0.45**
  - Continuing Bonds 내면화/외부화 → **risk_level 게이트 학술 근거**
  - 김윤한 국내 논문(KCI 비애 지속·VR 감정 정화)과 통합
- [x] **remote 자동화 상황 규명** — LP 본체(엔진+animals 모델+XPose ops)가 **장민수 PC에만** 존재 확인
  - 정환주 GPU엔 미설치(껍데기뿐) → `/health` "가짜 성공" 함정 확인
  - PM 결정: 발표 LP 호스트를 **반소람 PC**로 전환 (장민수 PC 안정성 이슈)
- [x] **소람 PC용 LP 설치 가이드 작성** (PR #171) — 장민수 PC 실측 구성 기반
  - 함정 3개 선제 차단: torch GPU별 버전 / XPose CUDA ops 빌드 / 가짜성공 방지
  - pipeline.py 호출 인자 대조 검증 + PowerShell 호환(curl.exe·cd 등)
- [x] **발표 사전 녹화 백업본 2종** 생성·검증 (`06_backup/`, 1인칭·3인칭 BGM, faststart)
- [x] dev 최신화 — BGM 확정본(bgm_1st/3rd)·TTS 업데이트 수신

### 막힌 것 / 고민
- 강사: 기능은 있으나 회복 "효과 입증" 구조 부재 → 논문근거 + 정량지표(play_count 등)로 대응, 내일 재논의
- LP 호스트 소람 PC 전환 → 소람 PC에 LP 새 설치 필요(수 GB + XPose 빌드), **발표 전 완료가 관건**
- 백업본 음성 없음(BGM만) — TTS 낭독 음성 확보 후 완전판 필요

### 내일 할 일
- [ ] 소람 PC LP 설치 지원 (SETUP_LivePortrait.md 따라) → §9 실제 생성 테스트까지
- [ ] TTS 낭독 음성 확보 → `merge_audio`로 음성+BGM 완전판 백업
- [ ] 발표 시연 리허설 + 터널 안정화 체크

---

## 2026-06-08 (Day 8)

### 오늘의 목표
- remote GPU 추론 동작 확인 (ROADMAP Phase 1 마감)
- 강사·PM 방향 피드백 반영 + 영상 톤·강도 탐색

### 한 일
- [x] dev 최신화·브랜치 동기화, PR #117·#120 머지 확인
- [x] **remote GPU 테스트** — 정환주님 고정 ngrok 터널로 사진 POST
  - 터널·통신·코드 경로 **정상 동작 확인** (health ok, 요청 도달)
  - GPU 서버 driving 영상 누락(`d0.mp4` 없음 → 400) 발견 → 정환주님 핸드오프 정리 (`LIVEPORTRAIT_DRIVING` 세팅 필요)
  - 고정 터널 URL을 `.env`에 기록 (`MODE=local` 유지, 세팅되면 remote 전환)
- [x] **XPose 입 개폐 자동선택 검증** (PR #120 머지) — driving 자동선택 전제 확인
  - 기본 설정 `animal_face_9`가 upper/lower lip 키포인트 추출 → lip_gap 측정
  - 입 벌림 0.646 vs 입 다뭄 0.07~0.13 = **약 5배 분리**, 추가 설치 0
- [x] 기능정의서 멀티모달 파트 검토 (다운로드 API 등 실제 구현 확인) + 보강안 정리(카톡 전달)
- [x] **ETHICS 가이드 갱신** (PR #124) — 추모 영상 윤리 PERSO→LivePortrait 발화 driving (규칙 유지, 메커니즘만)
- [x] 회복 게이트 보강 정리(텍스트 공유) — 중간 보상 "조각 모으기"를 50점 기준선에 통합 + 미디어 저장정책(다운로드+30일 정리+원본 사진 보관해 재생성)
- [x] **영상 톤·강도 실험** — 강사 "영상 퀄리티 핵심" 피드백 반영
  - ambient(multiplier 0.3) vs 발화(0.4~0.6) 비교 — 강도별 입 움직임 사다리 측정
  - 동물별 입 강도 편차 확인: 같은 0.5에 햄스터 1.57~셸티 4.22 (**종·얼굴크기별 보정 필요**)
  - driving 아바타 10종 랭킹: **d12(9.68)·d11(6.63, 단일클립 최매끄러움)** 이 움직임 최강

### 막힌 것 / 고민
- remote: GPU 서버 driving 영상 누락 → 정환주님 세팅 대기 중 (코드는 검증 완료)
- 같은 multiplier여도 종/얼굴 크기별 입 강도 편차 큼 → 종별 강도 보정값 필요 (자동선택 파라미터)
- Hallo2 검토: 동물 적용은 조건부 가능하나 audio-driven(ambient 목적과 불일치) + 품질 미검증 → 발표 후로 보류
- 컨셉 피벗(시한부 선고 진입점) PM 논의 중 → 생전=실기록 / 사후=추모영상 구분 필요

### 내일 할 일
- [ ] 정환주님 터널 driving 세팅되면 remote 재테스트
- [ ] 발표용 데모 영상 확정 (소스: 입 다문 정면 / driving: d11~d12 / multiplier: 0.5 전후)
- [ ] 종별 적정 multiplier 보정값 정리

---

## 2026-06-06 (Day 6)

### 오늘의 목표
- 방향 전환(PERSO 드랍 → LivePortrait 채택) 반영
- 발화 driving 영상 비교 테스트 (모세종님 요청)

### 한 일
- [x] dev 최신화 + 방향 전환 문서 파악 — PERSO 드랍 확정, LivePortrait(방법 3) 채택, TTS ElevenLabs 전환, ROADMAP 내 할 일 확인
- [x] 발화 driving 영상 폴더 생성 `ai/liveportrait/driving/` + README (영상은 `*.mp4` gitignore)
- [x] **driving 영상 7종 전수 비교** (calm 발화 4 + LP 기본 3) — 강아지(말티즈)·고양이(s39)에 적용
  - 입 영역 프레임 간 픽셀차로 입 움직임 강도·전환 스파이크 측정 스크립트 작성
- [x] **핵심 발견 3가지**
  - 소스 사진 입 모양이 결정적: 입 벌림=혀만 늘어남(말하는 티 X) / 입 다뭄=여닫음(O)
  - 단일 클립(d11) >> `_3clips`(전환 블러) — 스파이크/평균 1.7 vs 3.4~4.6
  - 발화 클립 최적 = EdMarkey0, 입 다문 소스엔 d11 범용 최적
- [x] **다른 동물 일반화** — 토끼·햄스터 d11로 자연스러운 발화 확인 (종 불문 동작)
- [x] **소스 사진 가이드 도출** — 입 다문 정면·얼굴 또렷·가림 없음 권장
- [x] `LIPSYNC_EXPERIMENT.md` 부록 A(실험 수치·발견) + 부록 B(driving 자동 선택 설계, XPose 재활용) 기록 → **PR #117**

### 막힌 것 / 고민
- 입 움직임 픽셀 지표는 "입 여닫음"과 "머리 움직임"을 구분 못 함 → 육안 확인 병행 필요
- 정환주님 GPU 터널 연결 대기 중 → remote 테스트 보류
- driving 자동 선택: XPose 키포인트로 입 개폐 추정 가능한지 검증 선행 필요

### 내일 할 일
- [ ] 정환주님 터널 뜨면 remote 원격 테스트 (사진→GPU서버→mp4)
- [ ] (회의에서 자동선택 진행 결정 시) XPose 키포인트 노출 검증 — 입 개폐 추정 가능 여부
- [ ] ElevenLabs TTS 나오면 LivePortrait 영상+음성 합치기 E2E

---

## 2026-06-05 (Day 5)

### 오늘의 목표
- LivePortrait 영상 + TTS 합치기 (발표 1순위)
- 멀티모달 파이프라인 서버 배포 구조 + PERSO 검증

### 한 일
- [x] **`merge_audio(video, audio)` 구현·검증** (PR #74 머지) — 무음 영상 + TTS mp3 → 음성 입힌 MP4
  - 영상 loop + 음성 길이 맞춤(`-stream_loop -1 ... -shortest`), libx264/yuv420p/aac, ffmpeg 폴백
  - 백엔드(모세종) `run_liveportrait`가 자동 호출하도록 연결됨 (PR #76)
- [x] **driving 경로 env 분리** (PR #83 머지) — `LIVEPORTRAIT_DRIVING` 으로 서버 배포 대비, 하위호환 유지
- [x] **PERSO 립싱크 실연동 검증** (PR #90·#91 머지) — 스크럼 "PERSO 미검증 ⬜" 해소
  - 동물 얼굴 립싱크 동작 확인(강아지·고양이), `perso_lipsync_test.py` 추가
  - `/lip-sync`는 GET 전용(405), `translate ko→ko`로 한국어 유지 → 모세종 `run_perso` en→ko 반영됨
  - **이빨 발견:** 입 다문 소스는 모델이 입속·송곳니 생성, 입 벌린 소스는 혀만(정정 PR #91)
  - 시각 이득 작음 → 발표 서사 "기술 가능→윤리 제한" 데이터로 뒷받침
- [x] **remote 모드 + `server.py`** (PR #95 머지) — 정환주 GPU 서버 터널 추론 구조
  - `LIVEPORTRAIT_MODE=remote` → 터널 URL로 사진 POST → 무음 mp4 수신 (로컬에서 E2E 검증 성공)
  - GPU 서버엔 추론만, merge_audio·PERSO는 백엔드(GPU 불필요)
- [x] **GPU 서버 세팅 가이드** (PR #96 머지) — README §5 재현 가이드(설치→XPose→server.py→터널→env)
- [x] **잔잔한 추모 driving 템플릿** — 프레임 평균(tmix) 방식 확정 (슬로우모션 느낌 없이 잔잔)
- [x] **PERSO 전수조사** — 계정 = 5팀 555198 / **KDT Biz Enterprise / 1,671 크레딧**(립싱크 3크레딧/초 → ~70회)
  - 워터마크 없음, 숨은 기능(STT·오디오분리·자막) 확인, 진짜 lip-sync 엔드포인트 경로(projectSeq) 규명
- [x] **팀 합의·파이프라인 순서 확정** — 기본=잔잔 영상+TTS / PERSO 립싱크=opt-in+"AI 재해석" 라벨(1인칭 발화 ❌), PERSO는 더빙·립싱크 전용(voiced 영상 필수)이라 순서 LivePortrait→TTS합치기→PERSO 고정, 발표 본방은 PERSO 미리 렌더 권장

### 막힌 것 / 고민
- 정환주님 GPU 서버 터널 연결 대기 중 → 연결되면 remote 원격 테스트 1회 필요
- Git: PR squash 머지로 분기 갈림 → 매번 `git merge origin/dev`로 정합 (반복 작업)

### 내일 할 일
- [ ] 정환주님 터널 뜨면 **remote 원격 테스트** (사진→GPU서버→mp4)
- [ ] E2E 시연 1회 완주 (회원가입→메시지→TTS→영상) — 반소람님 RAG corpus 완료 후
- [ ] (선택) PERSO TTS 모델 ELEVEN_V2→V3, 발표용 영상 미리 렌더

---

## 2026-06-04 (Day 4)

### 오늘의 목표
- LivePortrait 검증 마무리 (종별·조건별)
- 프론트 담당 페이지 구현

### 한 일
- [x] LivePortrait driving_multiplier 0.4 확정 (0.5·0.65·0.8 비교 후 추모 톤에 맞는 0.4 선택, eyes 모드 동물 무효 확인·정정)
- [x] 종별 검증: 고양이(랙돌)·강아지(시바·골든) 품질 확인
- [x] 까다로운 조건 검증: 어두움·저화질·작게 찍힌 사진 — 얼굴 검출 모두 성공
- [x] 실제 반려동물 사진 검증: 비숑·셸티·말티즈 3종 성공
- [x] 비전형 동물 일반화 검증: 토끼·햄스터·앵무새·올빼미·말·금붕어·도마뱀 7종 모두 검출·생성 성공 (no face 에러 0건)
- [x] 한글 경로 버그 발견 및 기록 (cv2.imread Windows 비ASCII 경로 실패)
- [x] output/ 폴더 영어로 정리 (cat/, dog/, dog/hard_cases/)
- [x] EXPERIMENT.md 검증 결과 전체 기록
- [x] `TtsPage.jsx` · `TimelinePage.jsx` · `MediaPage.jsx` 구현 (PR #53 머지)
- [x] TTS API 경로 수정: `POST /messages/{id}/tts` → `POST /tts` (PR 코멘트 반영)
- [x] 서버 연결 `.env` 설정 (VITE_API_BASE_URL)

### 막힌 것 / 고민
- MediaPage: 백엔드 `/media/upload` API 미구현 (모세종님 예정)
- TtsPage: `message_content` localStorage 저장 필요 (민경이님께 요청)
- Git: PR #65가 squash 머지되며 직후 푸시한 개발일지 커밋이 dev에 누락 → squash는 분기 이력을 갈라놓아 별도 docs PR(#69)로 분리해야 했음. `git merge origin/dev` 로 동기화 후 해결
- Git: 개발일지 `README.md` 머지 충돌(팀원들이 같은 Day 4 표 동시 갱신) → dev 최신 줄 유지 + 장민수 줄만 합쳐 해소

### 내일 할 일
- [ ] TimelinePage 실서버 연결 테스트
- [ ] MediaPage 백엔드 API 나오면 연동
- [ ] TtsPage 민경이님 message_content 저장 후 실서버 테스트

---

## 2026-06-02 (Day 2)

### 오늘의 목표
- 레포 전체 구조 파악 (md 파일 전체 읽기)
- 프론트엔드 작업 환경 세팅 (Vite + React)
- FRONTEND_PLAN.md 기반 역할 분담 확인

### 한 일
- [x] 레포 전체 md 파일 읽고 구조 파악 (README, ARCHITECTURE, PROGRESS, CONTRIBUTING, FRONTEND_PLAN, ai/ 문서 8종)
- [x] FRONTEND_PLAN.md 확인 — 담당 기능: ④TTS, ⑤미션, ⑥타임라인, 가산점(MediaPage)
- [x] Vite + React 환경 점검 (react-router-dom, axios, tailwindcss 설치 테스트) → **세팅은 민경이님이 먼저 push 담당이라 로컬 파일은 롤백**
- [x] `git pull origin dev` 로 AI 파트 신규 문서 수신 (ai/CLAUDE·ROLES·TODO·GPU_SERVER 등) + FRONTEND_PLAN.md 머지 충돌 해결
- [x] `ai/liveportrait/README.md` 생성 (LivePortrait 파이프라인 문서화)

### 알게 된 것 / 결정
- **GPU 제약 확인 (GPU_SERVER.md):** RTX 5060 = VRAM 8GB. LLM 7B(Q4) 상시 ~5~6GB 점유 → LivePortrait(4~6GB)와 **동시 상주 불가.**
  → 결론: 개발 단계에서는 **Replicate fallback 우선**으로 잡고, 로컬 GPU는 정환주님과 시간대 협의해서 사용.
- LivePortrait 위치 명확화: `ai/liveportrait/`는 멀티모달(장민수) 담당, AI 파트(반소람·정환주) 범위 밖. GPU 자원만 정환주님과 공유.
- 세팅 순서 합의 필요: FRONTEND_PLAN.md상 민경이님 push → 장민수 pull. 내 로컬 Vite는 정리함(충돌 방지).

### 막힌 것 / 고민
- FRONTEND_PLAN.md 기준 JS(.jsx)인데 npm 기본은 TS(.tsx) → 민경이님과 **언어 통일** 결정 필요
- LivePortrait는 **사람 얼굴 기준 모델** → 강아지/고양이 사진엔 animals 모드/모델 필요 여부 검증 필요 (README에 기록)
- 정환주님 GPU 서버 가동 시간/접근 방법 미확인 → 로컬 테스트 일정 불투명

### LivePortrait 환경 세팅 + Animals 모드 완료 (추가)
- [x] LivePortrait 레포 클론 (`c:\JMS\2_project\LivePortrait\` — 레포 밖)
- [x] conda 환경 생성 (liveportrait, Python 3.11)
- [x] PyTorch nightly cu132 재설치 (시스템 CUDA 13.2와 버전 맞추기)
- [x] onnxruntime-gpu → onnxruntime(CPU)로 교체 (cuDNN DLL 문제)
- [x] Visual C++ Build Tools 설치
- [x] ninja 설치
- [x] XPose CUDA 확장 컴파일 성공 (`MultiScaleDeformableAttention.cp311-win_amd64.pyd`)
- [x] 사전학습 가중치 다운로드 (humans + animals)
- [x] **Humans 모드 동작 확인** → `animations/s0--d0.mp4`
- [x] **Animals 모드 동작 확인** → 고양이 `s39.jpg` → `animations/s39--d0.mp4` + `.gif` ✅
- [x] 팀 보고 완료

### 해결 과정 요약 (재현용)
PyTorch cu124 → cu132 nightly 재설치 → MSVC 설치 → cpp_extension.py 인코딩 패치 → setup.py 전처리기 플래그 추가 → ninja 설치 → pip install -e . 로 최종 설치. 자세한 내용: [ai/liveportrait/README.md §10](../../ai/liveportrait/README.md)

### 강사님 스크럼 피드백 (2026-06-02)

**모델**
- EXAONE 대신 **Gemini** 사용 권장
- 실시간 서비스는 어려울 것 같다고 하심
- 이 서비스에서 RAG를 어떻게 활용할지 고민 필요

**LivePortrait**
- 베이스 사진·영상 퀄리티가 결과물에 직접 영향 → driving 템플릿 신경 써야 함
- "동물 눈·코·입 움직이는 정도만 돼도 충분" → 현재 방향(잔잔한 움직임) 유효

**서비스 시나리오 (최우선)**
- 기능은 있지만 서비스 연결점이 약하다고 하심
- 사진 올리면 잊었던 추억이 나오는 흐름 연결하면 좋다고 하심
- 방향 3가지 중 하나로 확실히 결정 필요:
  1. 장례 종합서비스
  2. 건강관리 + 추모 버튼
  3. 다른 방향
- "전체 프레임 먼저 잡고 기능 골라라"

**⏰ 데드라인: 서비스 시나리오 2026-06-05(목)까지 확정**

### 서비스 시나리오 확정
- 팀 시나리오 확정: **"아플 때부터 함께, 떠난 후에도 함께"** (3단계 라이프사이클)
  - 1단계 건강관리(증상→AI 안내·병원 검색·기록), 2단계 장례 안내, 3단계 추모 모드("추모하기" 버튼으로 기존 기록 → 추모 타임라인 전환)
- 멀티모달(장민수) 담당: **3단계 추모 모드 집중** — 추모 영상(LivePortrait), TTS 플레이어, 사진 업로드, 타임라인 UI
- 일정: 1·2단계 6/5~6/10, 3단계 6/10~6/15, 최종 마감 6/17

### 내일 할 일
- [ ] driving 모션 템플릿 준비 (잔잔한 눈 깜빡임·끄덕임) — 강사 피드백 "베이스 영상 퀄리티 중요"
- [ ] 추모 영상 파이프라인 함수화 (사진 입력 → MP4 출력)
- [ ] 민경이님과 Vite 세팅 조율 → 세팅 들어오면 `npm install`
- [ ] Replicate fallback 구현
- [ ] 정환주님께 GPU 서버 슬롯 협의
