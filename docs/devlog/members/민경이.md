# 개발일지 — 민경이 (프론트엔드)

> 최신 날짜를 **맨 위**에 추가하세요. 통합 일지: [../README.md](../README.md)
>
> <details><summary>📋 작성 템플릿 (복사해서 사용)</summary>
>
> ```markdown
> ## YYYY-MM-DD (Day N)
> ### 오늘의 목표
> -
> ### 한 일
> - [x]
> ### 막힌 것 / 고민
> -
> ### 내일 할 일
> - [ ]
> ```
> </details>

---

## 2026-06-16 (Day 16)

### 오늘의 목표
- 코드 리뷰 버그 11건 수정
- 미션 난이도 시스템·기념일 케어 프론트 연동
- 회복 리포트 개선 + OTA 배포 정상화

### 한 일
- [x] **OTA(EAS Update) 배포 정상화**
  - dev-client 빌드는 preview 채널 OTA를 못 받음을 확인 → preview 빌드 APK 필요
  - 세종님 새 계정(aaed6170)으로 app.json owner·projectId·updates.url 통일 확인
  - 홈 버전 푸터로 OTA 수신 검증 후 제거
- [x] **오늘의 미션 난이도 시스템 연동 (소람님 LLM)**
  - difficulty(gentle/small/active) 배지 표시 — 가볍게 / 한 걸음 더 / 활동
  - 건너뛰기(skip) 버튼 → `PATCH /missions/{id}/skip` 연동, 대체 미션 교체
  - 점수·레벨 계산은 백엔드 담당, 프론트는 difficulty만 표시 (셋이 어긋나지 않게)
- [x] **감정 체크인 수면 질 입력 추가**
  - '어젯밤 수면' 5단계 선택 → `postEmotion`에 `sleep_quality` 전달
  - 소람님 `mission.py` `_apply_sleep` 난이도 보정과 연결
- [x] **회복 리포트 개선**
  - 작동 안 하던 '스마트폰 사용 패턴'(usage-stats) 제거
  - 삼성헬스 걸음·수면 표시 (health 연동 결과 캐시)
  - 미션 완료율을 `mission.jsx`와 동일 로직(오늘 기준)으로 계산
- [x] **코드 리뷰 버그 11건 수정** (BUG-04는 장민수님 담당)
  - BUG-03 게이트 확인 실패 시 fail-closed(locked)
  - BUG-05 teaser `content_unlocked` 확인 후 편지 노출 (장민수님 recovery.js 연동)
  - BUG-06 미션 완료 API 실패분 재진입 시 서버 재동기화
  - BUG-07 GIF 여정 노드 `hasGif` 체크 (+ MemorialHome 호출부 누락 보완)
  - BUG-08 1인칭 요청에 `first_person≠true` 시 3인칭 무소음 노출 차단
  - BUG-10 TTS teaser 진행바 45점 기준 재계산
  - BUG-11 미션 완료 날짜 키 서버 UTC 기준 통일
- [x] **기념일 케어(care) 프론트 연동** (BUG-02/09)
  - `api/care.js` 신설, 이별 후 홈에 D+30·D+100 케어 카드 표시
- [x] **추모 편지 안내 문구 정리**
  - 편지 끝 'AI가 생성한 메시지입니다' 제거
  - 위로 편지(3인칭) 문구 변경, 별에서 온 편지(1인칭)는 윤리 필수 경고("실제 반려동물이 직접 한 말은 아니에요") 병기
- [x] **이별 후 홈 더보기 카드 정리**
  - '음성으로 듣기' 카드 제거, '추모 영상 만들기'를 가로 작은 카드로 (메인/보조 위계 구분)
- [x] **상단 로그아웃 중복 제거** (report·mission — 하단바와 중복, message.jsx는 유지)

### 막힌 것 / 고민
- 버킷리스트 키보드 회피: iOS는 `KeyboardAvoidingView` `padding`으로 동작, Android는 시스템 resize 의존 — Expo SDK 54 edge-to-edge 영향으로 JS만으론 보장 어려움. 확실히 하려면 `app.json` `softwareKeyboardLayoutMode` 명시 + 재빌드 필요
- `report.jsx` 수면 시계열 차트: 백엔드 `ReportResponse`에 `sleep_trend` 없음 → 세종님 추가 필요

### 내일 할 일
- [ ] 버킷리스트 Android 키보드 — 폰 테스트 후 필요 시 app.json 재빌드 요청
- [ ] 발표(6/19) 대비 시연 흐름 점검

---

## 2026-06-15 (Day 15)

### 오늘의 목표
- 팀 코드 리뷰(PR #303) 피드백 버그 수정
- 삼성헬스 연동 팀원 배포 준비 (EAS Build)
- 홈 화면 헤더 UX 개선

### 한 일
- [x] **프론트 버그 6건 수정 (민수님 코드 리뷰 피드백 반영)**
  - `recovery.js`: 캐시 폴백 시 `gate_status` 우선 사용 (API 성공 시 저장된 값 활용)
  - `message.jsx`: `initGate()` try/catch 추가 → pet_id 없거나 네트워크 실패 시 로딩 무한 방지, `cleanup()` ref null 먼저 처리 → BGM 이중 언로드 방지
  - `tts.jsx`: "다시 생성" 연속 탭 race condition 수정 — `soundRef.current = null` 선처리 후 stop/unload await
  - `memories.jsx`: `caller_name`을 `guardian_title`(호칭)로 통일, `profile.caller_name` 덮어쓰기 제거
  - `_layout.jsx`: `doLogout()` 로그아웃 시 `tts_done` 키 미삭제 버그 수정
- [x] **추가 버그 5건 수정**
  - `emotion.jsx`, `gift.jsx`: 반려동물 미등록 상태 진입 시 `pet_id: null`로 API 호출하는 버그 수정
  - `mission.jsx`: `useEffect` → `useFocusEffect` 교체 (화면 재진입 시 미션 목록 새로고침)
  - `photos.jsx`: `pet_id` 없을 때 서버 업로드 요청 스킵 → 불필요한 422 방지
  - `message.jsx`: 1인칭 메시지가 3인칭 위로 편지에 재사용되는 버그 수정
- [x] **홈 화면 헤더 UX 개선**
  - 이별 전 홈: 상단 "홈" 제목 제거 (`headerShown: false`)
  - 이별 후 홈: 헤더 유지, 뒤로가기 버튼 텍스트 "뒤로"로 변경
  - 전체 화면 뒤로가기 버튼 텍스트 "홈" → "뒤로" 전역 변경 (`_layout.jsx`)
- [x] **버킷리스트 키보드 UX 개선**
  - 안드로이드에서 키보드 올라올 때 입력창 위에 갭 생기는 버그 수정
  - `kbHeight + marginBottom` 이중 적용 제거 → `KeyboardAvoidingView`로 교체
- [x] **백엔드 피드백 대응**
  - `memories.jsx` 사진 업로드 미도달 원인 분석: `pet_id` null 조건 버그 → 수정
  - `POST /api/v1/auth/login 422`: 프론트 코드 형식 정상 확인 (`{ email, password }` JSON), 백엔드 공유
- [x] **EAS Build 한도 소진 확인 및 대안 안내**
  - 이달 Android 빌드 한도 소진 (리셋 7/1) → 팀원(세종님) 계정으로 빌드 방법 안내

### 막힌 것 / 고민
- EAS 무료 플랜 Android 빌드 한도 소진 → 7/1 리셋 전까지 팀원 계정으로 빌드 필요
- `report.jsx` sleep_trend 필드 백엔드 미구현 → 세종님·정환주님과 협의 필요
- `media.jsx` GIF 표시 조건 (`recoveryScore < 20`) → 민수님 담당 확인 중

### 내일 할 일
- [ ] 세종님 EAS Build 완료 후 APK 팀원 배포 확인
- [ ] 발표(2026-06-19) 시연 흐름 최종 점검
- [ ] `report.jsx` sleep_trend 백엔드 연동 결과 확인

---

## 2026-06-11 (Day 11)

### 오늘의 목표
- 강사님 요청 — Expo SDK 52 → 54 업그레이드 (Android + iOS 모두 최신 Expo Go 지원)
- SDK 54 업그레이드 트러블슈팅 문서 작성
- 추모 메시지 봉투 애니메이션 미표시 버그 수정

### 한 일
- [x] **Expo SDK 52 → 54 업그레이드** (`package.json`, `app.json`)
  - React 18 → **19.1.0**, React Native 0.76.7 → **0.81.5** 로 상향
  - `npx expo install --fix` 로 전체 호환 버전 자동 재정렬 (expo-router 4→6, expo-av 15→16, expo-font 13→14 등)
  - `react-dom` peer dep 충돌 → `npm install --legacy-peer-deps` 로 해결
  - `app.json`: iOS `NSAllowsArbitraryLoads: true`, Android `usesCleartextTraffic: true`, `expo-font` 플러그인 추가
  - 결과: **App Store / Play Store 최신 Expo Go 로 iOS·Android 모두 바로 실행 가능**
- [x] **SDK 54 breaking change 대응**
  - `photos.jsx`, `media.jsx`: `ImagePicker.MediaTypeOptions.Images` → `['images']` (expo-image-picker 17.x deprecated)
  - `message.jsx`: `pointerEvents` 를 StyleSheet 속성에서 **컴포넌트 직접 prop** 으로 분리 (React Native 0.81.5 breaking change)
- [x] **트러블슈팅 문서 신규 작성** (`frontend-rn/Design/SDK54_Troubleshooting.md`)
  - 발생한 오류 3건(datetimepicker 버전 없음 / runtime not ready / peer dep 충돌) + 예상 오류 3건 + iOS·Android 실행 가이드 + 클린 재설치 절차 수록
- [x] **추모 메시지 봉투 애니메이션 버그 수정** (`message.jsx`)
  - 원인: 백엔드가 `source: 'unavailable'` 반환 → `setPhase('letter')` 직행, 봉투 건너뜀
  - 수정: `loadMessage()` 에서 `source === 'unavailable'` 감지 시 `throw` 처리 → 최종 로컬 폴백(`makeFallbackMessage`) 사용
  - 효과: 백엔드 LLM 비가용 상태에서도 봉투 애니메이션 + 기본 편지 정상 표시

### 막힌 것 / 고민
- `npx expo install` 단독 실행은 구버전 호환표 참조 → `--fix` 플래그 필수 (안 쓰면 RN 버전 불일치로 `runtime not ready` 크래시)
- 봉투 미표시 근본 원인은 백엔드 LLM 응답 실패 — 백엔드 Gemini 연결 상태 안정화 시 폴백 없이 실제 메시지 표시 가능

### 내일 할 일
- [ ] 전체 E2E 재테스트 (SDK 54 환경, 로그인 → 홈 → 감정 → 추모 메시지 봉투 → TTS → 미션)
- [ ] iOS Expo Go 테스트 확인
- [ ] 발표(2026-06-19) 시연 흐름 최종 점검

---

## 2026-06-10 (Day 10)

### 오늘의 목표
- UI_흐름_설계.md(세종님 작성) 반영 — 생존 모드 / 이별 후 모드 홈 분리
- 온보딩 흐름(profile → memories → home) 완전 연동
- 신규 가입자 버그 수정 + API 경로 오류 수정

### 한 일
- [x] **홈 화면 생존/이별 모드 분리** (`home.jsx`)
  - 생존 모드 (`memorial_mode=false`): 버킷리스트·일기 BigCard, 사진·증상 SmallCard, 하단 "무지개다리를 건넜어요 🌈" 버튼
  - 이별 후 모드 (`memorial_mode=true`): 감정 체크인·추모 편지(회복 게이트)·오늘의 미션 BigCard, 타임라인·리포트 SmallCard
  - 이별 전환 확인 모달 → AsyncStorage `memorial_mode=true` 저장
  - 반려동물 카드에 이별 후 D+N 일수 표시
- [x] **온보딩 프로필 데이터 홈 연동** (`memories.jsx`, `home.jsx`)
  - `memories.jsx`: `pet_guardian_title`, `pet_gender`, `pet_start_date` AsyncStorage 저장 추가
  - `home.jsx`: 펫 카드에 성별·종·함께한 기간(`start_date ~ end_date`)·호칭 표시
  - `login.jsx`: 재방문자 로그인 시 `gender`, `start_date` 복원 추가
- [x] **신규 가입자 프로필 강제 이동 버그 수정** (`register.jsx`, `login.jsx`)
  - 신규 가입 후 로그인 시 백엔드 기존 pet 데이터 무시하고 profile.jsx로 강제 이동
  - `register.jsx`: 가입 성공 시 `new_registration` 플래그 AsyncStorage 저장
  - `login.jsx`: 플래그 감지 시 기존 pet 데이터 전체 초기화 후 profile로 이동, 재방문자는 기존 로직 유지
- [x] **missions API 경로 수정** (`api/missions.js`) — 세종님 피드백
  - `/api/v1/missions?pet_id=${pet_id}` → `/api/v1/missions/${pet_id}`
- [x] **PATCH /api/v1/pets 405 오류 제거** (`home.jsx`)
  - 백엔드 미구현 엔드포인트 호출 제거, memorial_mode는 AsyncStorage 로컬 저장만 유지
- [x] **dev 최신화 + PR 제출** (2회) — 충돌 없이 머지 완료

### 막힌 것 / 고민
- 신규 가입자가 기존 계정 데이터를 보는 문제 → 백엔드 테스트 데이터 잔존이 원인, 프론트 플래그로 우회 처리
- memorial_mode PATCH 엔드포인트 백엔드 미구현 → 로컬만 저장, 추후 세종님과 연동 협의 필요

### 내일 할 일
- [ ] 전체 E2E 테스트 (신규 가입 → 프로필 → 추억 → 홈 → 이별 모드 전환)
- [ ] PR 리뷰 반영 후 dev 머지 확인

---

## 2026-06-09 (Day 9)

### 오늘의 목표
- PM 작업지시서 프론트 항목 반영
- 버킷리스트·일기 신규 화면 구현
- 테스트 중 발견된 UX 버그 수정

### 한 일
- [x] **추모 메시지 봉투 애니메이션** (loading → envelope → letter 3단계 흐름)
  - `flapAnim` interpolate: flap 위로 올라가며 투명화
  - `envelopeAnim` 페이드아웃 + `paperOpacity`·`paperTranslate` 슬라이드업 동시 실행
  - 정환주님 dev 버전 기반으로 welfare 섹션 충돌 병합
- [x] **상담 자원 섹션(welfare_resources)** — `message.jsx`
  - `risk_level 1`: `support_message` 배너 + welfare_resources 카드
  - `risk_level 2`: `crisis_message` 배너 (SafetyModal 별도 유지)
  - `featured: true` → 상시 노출 / `false` → "더 보기" 토글
- [x] **미션 카드 rationale·category 아이콘** — `mission.jsx`
  - 제목 앞 `📋`, rationale 앞 `💡 rationale — (category)` 표시
  - `mockMissions` 샘플 데이터도 신규 필드 반영
- [x] **버킷리스트 화면 신규** (`bucketlist.jsx`)
  - 홈 → 생전 기록 → 버킷리스트 진입
  - pet API에서 `bucket_list` 불러오기 (없으면 샘플 3개 자동)
  - 항목 추가/체크, AsyncStorage 영속 저장
- [x] **일기 화면 신규** (`diary.jsx`)
  - 날짜별 일기 작성, 목록 역순 표시, AsyncStorage 저장
- [x] **home.jsx 생전 기록 카드 분리**
  - "버킷리스트 & 다이어리" 1개 → "버킷리스트" + "일기 & 추억 메모" 2개
- [x] **ForwardRef 에러 수정** (`memories.jsx`, `memories_diary.jsx`)
  - 렌더 중 `router.replace()` 호출 → `useEffect`로 이동
- [x] **재방문자 데이터 잔존 수정** (`login.jsx`)
  - 로그인 시 `AsyncStorage.multiRemove` (pet_id, pet_name, pet_species 등 초기화)
  - `getMyPets()` API로 현재 유저 펫 확인 후 설정
- [x] **키보드 가림 수정** (login, bucketlist, diary)
  - login: `ScrollView` 감싸기로 폼 스크롤 가능
  - bucketlist: 입력창을 `ScrollView` 밖으로 분리 → 키보드 위에 고정
- [x] **내비게이션 개선** (`_layout.jsx`)
  - 모든 앱 화면 헤더 우측에 "홈" 버튼 추가
  - 홈 화면 헤더 우측에 "로그아웃" 버튼 추가 + AsyncStorage 전체 클리어

### 막힌 것 / 고민
- Expo Go SDK 자동업데이트(52→54) 문제 발생 → Play Store 자동업데이트 차단, SDK 52 APK 재설치로 해결
- welfare_resources 백엔드 연동은 API 응답에 필드 포함 시 자동 표시 예정 (현재 백엔드 미완성)

### 내일 할 일
- [ ] E2E 전체 흐름 최종 테스트 (로그인 → 홈 → 감정 → 메시지 → TTS → 미션)
- [ ] 회복 게이트 UI 연동 (`content_unlocked` 버튼 활성화)
- [ ] 1인칭 편지 조건부 버튼 (`allow_first_person` 조건)
- [ ] 이별 진행 화면 구현 (고정 텍스트 카드 4섹션)

---

## 2026-06-08 (Day 8)

### 오늘의 목표
- 강사님 피드백 반영 — 프론트엔드 React Native + Expo로 전환
- 전체 화면 재구현 및 Expo Go 실기기 테스트
- 백엔드 Docker 연결

### 한 일
- [x] **플랫폼 전환 결정** — Vite+React+Capacitor → React Native + Expo SDK 52
  - 강사님(이석창): 레인보우 브릿지는 모바일 앱이 맞다는 최종 피드백 반영
  - `frontend-rn/` 폴더 신규 생성 (기존 `frontend/` 보존)
- [x] **앱 아이콘 교체** — 민수님 마지막사진(레인보우브릿지 고양이+강아지 일러스트)
  - `assets/images/icon.png`, `adaptive-icon.png`, `splash-icon.png` 에 적용
- [x] **프로젝트 초기 설정**
  - `package.json` 버전 핀 정리 (react@18.3.1, react-native@0.76.7, async-storage@2.1.0)
  - `npm install --legacy-peer-deps` 로 892개 패키지 설치 완료
  - `app.json` — 앱 이름, 번들 ID, 스플래시 배경색(`#F2EEE5`) 설정
  - `constants/colors.js` — PastelTone 기반 색상 팔레트 정의
- [x] **전체 화면 구현** (Expo Router v4 파일 기반 라우팅)
  - `(auth)`: login, register
  - `(app)`: profile, memories, emotion, message, tts, mission, timeline, report, funeral, symptoms, health-records, media
  - 공통 컴포넌트: Button, Card, LoadingSpinner, SafetyModal (1393 위기 연결)
- [x] **API 레이어 전환**
  - `localStorage` → `AsyncStorage`
  - `<audio>` → `expo-av`
  - `<input type="file">` → `expo-image-picker`
  - `href="tel:1393"` → `Linking.openURL('tel:1393')`
- [x] **Expo Go 실기기 테스트** (Android SM-S918N)
  - Play Store에서 Expo Go 2.32.20 설치
  - QR 스캔으로 앱 로드 확인
  - 회원가입·로그인 UI 전체 화면 렌더링 확인
- [x] **백엔드 Docker 연결**
  - `JWT_SECRET_KEY` 루트 `.env` 에 추가
  - `docker compose -f backend/docker-compose.yml up -d --build`
  - `rainbow_backend`, `rainbow_mongo`, `rainbow_redis` 모두 Running 확인
  - `http://localhost:8000/health` → `{"status":"ok"}` 확인
  - 프론트 `.env` 에 `EXPO_PUBLIC_API_URL=http://192.168.0.16:8000` 설정
- [x] **버그 수정** — `api/auth.js` register 함수 `name` → `nickname` 필드 수정 (백엔드 스키마 맞춤)
- [x] **README.md** 작성 — `frontend-rn/` 팀원 온보딩 가이드 (Expo Go 사용법 포함)
- [x] **PR 제출** — `minkyeongyi` → `dev` (React Native 전환 내용 포함)

### 막힌 것 / 고민
- Python 3.14 전역 설치로 pip 패키지 빌드 오류 → Docker로 해결 (로컬 Python 버전 무관)
- 포트 8000 충돌 → 기존 flask_app 컨테이너 중지 후 해결
- Expo 포트 8081 충돌 → 8082로 대체 실행

### 내일 할 일
- [ ] 회원가입 → 로그인 → 프로필 → 감정 전체 흐름 E2E 테스트
- [ ] 백엔드 실연결 후 각 화면 API 동작 확인
- [ ] 장민수님과 `tts.jsx`, `media.jsx` 연동 협의

---

## 2026-06-05 (Day 5)

### 오늘의 목표
- 서비스 시나리오 1단계(건강관리) · 2단계(장례 안내) 화면 구현
- 모바일 반응형 버튼 수정
- dev 최신화 후 PR 정리

### 한 일
- [x] **SymptomsPage** — AI 안내 결과 아래 주변 동물병원 mock 카드 3종 추가
  - 응급 뱃지, 카카오맵 지도 버튼, 전화 링크
  - "투약·검진 기록 관리" 버튼 → `/health-records` 이동 추가
- [x] **HealthRecordsPage** 신규 구현 (`/health-records`)
  - 투약 / 검진 탭 전환
  - 기록 추가 폼 (투약: 약이름·복용량·횟수·시작일·메모 / 검진: 날짜·검사항목·결과·메모)
  - 타임라인 카드 UI + 삭제 버튼
  - localStorage 저장·불러오기 (`health_medications`, `health_exams`)
- [x] **FuneralPage** 신규 구현 (`/funeral`)
  - 장례식장 찾기 탭: mock 장례식장 3곳, 카카오맵 링크, 전화번호
  - 절차 상담 탭: 선택지→AI 답변 트리 채팅 UI (비용·방식·시기·가족 4주제)
  - useRef 채팅 자동 스크롤
  - "추모하기" 버튼 → `/message` (3단계 진입)
- [x] **App.jsx** — `/health-records`, `/funeral` 라우트 추가
- [x] **MessagePage** — 린트 수정
  - `useCallback` 제거 → `useEffect` 내 `async function load()` + `cancelled` 플래그 패턴으로 교체
  - `react-hooks/set-state-in-effect` 경고 0건
- [x] **모바일 반응형** 수정 — EmotionPage · TtsPage · MissionPage · MediaPage 주요 버튼 `w-full` 추가
- [x] **hospitals.js / funerals.js** API 클라이언트 파일 생성
  - 다음 주 백엔드 `/api/v1/hospitals`, `/api/v1/funerals` 완성 시 바로 연동 가능하도록 준비
- [x] **dev 머지 + 충돌 해결**
  - LoginPage 충돌 — PM(모세종) 지시에 따라 dev 버전(`getMyPets()` 라우팅 로직) 채택 (`git checkout --theirs`)
  - 충돌 없는 나머지 파일은 자동 머지 완료
- [x] **PR #84** 제목·설명 업데이트 후 push (minkyeongyi → dev)
- [x] **플랫폼 전략 확정** — Android Capacitor 앱 + 웹(병원용) 이중 배포 결정
  - React Native 재작성 ❌ (일정 불가), PWA → Capacitor로 전환 (앱 아이콘·설치 요구사항 충족)
  - iOS는 Mac 없음으로 이번 MVP 제외
- [x] **FRONTEND_PLAN.md** 업데이트 — 플랫폼 전략·Capacitor 세팅 일정 반영

### 막힌 것 / 고민
- 장례식장·병원 데이터는 현재 mock — 백엔드 API 완성 후 `hospitals.js`/`funerals.js` 연결 필요
- FuneralPage 절차 상담은 선택지 트리 (실제 AI 호출 아님) — 반소람님 AI 연동은 추후 협의
- Capacitor Android 세팅 시 Android Studio + JDK 17 설치 선행 필요 (2주차 예정)

### 내일 할 일
- [ ] 오늘 커밋 push 정리 + PR #84 리뷰 요청
- [ ] 개발일지 통합 요약 업데이트
- [ ] 다음 주 Capacitor 세팅 준비 (Android Studio 설치 확인)

---

## 2026-06-04 (Day 4)

### 오늘의 목표
- EmotionPage score 매핑 수정 (백엔드 스키마 맞춤)
- ⑤ MissionPage 구현
- ⑧ ReportPage 구현
- 로그인·회원가입 페이지 구현
- 실서버 연결 테스트

### 한 일
- [x] EmotionPage `mood(string)` → `score: int` 변환 수정
  - 괜찮아요=9, 슬퍼요=6, 많이힘들어요=3, 너무힘들어요=1, 잘모르겠어요=5
- [x] `api/messages.js` 엔드포인트 `/messages/generate` → `/messages` 수정 (백엔드 실경로 맞춤)
- [x] **⑧ ReportPage** 구현
  - 감정 추이 LineChart (recharts), 미션 완료율 프로그레스 바, 이용 요약 카드 3종
  - `api/report.js` 신규 — `GET /api/v1/report/{pet_id}`, `mockReport` 추가
- [x] **⑤ MissionPage** 구현
  - 미션 카드 목록, 완료율 프로그레스 바, 완료 버튼 (mock fallback 포함)
  - `api/missions.js` 신규 — `getMissions()`, `completeMission()`
- [x] mock 스키마를 백엔드 응답 구조에 맞게 수정 (`mockEmotion` score/risk_level, `mockMissions` completed/description)
- [x] **LoginPage · RegisterPage** 구현 (백엔드 `POST /api/v1/auth/login·register` 연동)
  - `api/auth.js` 신규
  - `axiosInstance.js` 요청 인터셉터 추가 — 매 요청에 `Authorization: Bearer <token>` 자동 삽입
  - 앱 진입점 `/` → `/login` 변경
- [x] `TtsPage.jsx` `audio_url` 상대경로(`/uploads/...`) → 절대경로 변환 수정
- [x] 실서버(`101.79.19.87:8000`) `.env` 연결 후 전체 흐름 동작 확인

### 막힌 것 / 고민
- TTS 낭독 실연결은 정환주님 Google Cloud TTS 키 서버 설정에 의존 — 키 배포 후 재테스트 필요
- ⑥ 타임라인 화면은 백엔드 API 확인 후 구현 예정

### 내일 할 일
- [ ] ⑥ TimelinePage 구현 (타임라인 API 연동)
- [ ] 실서버 E2E 테스트 (회원가입→로그인→프로필→감정→메시지→TTS→미션 전체 흐름)
- [ ] ProfilePage 백엔드 연동 확인 (pet_name localStorage 저장 여부)

---

## 2026-06-03 (Day 3)

### 오늘의 목표
- ② EmotionPage 감정 체크인 페이지 구현
- ③ MessagePage AI 추모 메시지 카드 구현
- axiosInstance ngrok URL 하드코딩 → 환경변수 전환 (코드리뷰 반영)

### 한 일
- [x] **② EmotionPage** 구현
  - 감정 5종 이모지 버튼(괜찮아요·슬퍼요·많이 힘들어요·너무 힘들어요·잘 모르겠어요)
  - 선택적 메모 입력칸
  - `risk_level >= 2` 또는 '너무 힘들어요' 선택 시 SafetyModal 자동 표시
  - 백엔드 미연결 시 mock 처리 → 위험 감정만 모달, 나머지는 `/message` 이동
- [x] `api/emotions.js` — `postEmotion()` / `POST /api/v1/emotions` 작성
- [x] **③ MessagePage** 구현
  - 페이지 진입 시 자동 메시지 생성 API 호출
  - 로딩 중: 스피너 + "콩이의 추억을 떠올리고 있어요..." 텍스트
  - 완료: 보라색 카드에 메시지 표시 + [다시 생성] / [음성으로 듣기] 버튼
  - 백엔드 미연결 시 mock 데이터 자동 대체
- [x] `api/messages.js` — `generateMessage()` / `POST /api/v1/messages/generate` 작성
- [x] `axiosInstance.js` baseURL `VITE_API_BASE_URL` 환경변수로 교체 (모세종님 코드리뷰 반영)
- [x] `frontend/.env.example` 추가
- [x] PR #28 (모세종 백엔드) Approve — 프론트 스키마 영향 확인 후 승인
- [x] git stash / merge dev / conflict 해결 / push 작업

### 막힌 것 / 고민
- 백엔드 서버 없이 mock 데이터로 개발 중 — 실제 API 연동은 2주차 예정
- git 브랜치 작업 흐름이 복잡했음 → 앞으로는 dev pull 먼저 하고 시작하기

### 내일 할 일
- [ ] ⑧ ReportPage 구현 (`recharts` 설치, 감정 변화 그래프·미션 완료율)
- [ ] EmotionPage·MessagePage 커밋/push 정리
- [ ] ProfilePage 스키마 PR #28 반영 확인

---

## 2026-06-02 (Day 2)

### 오늘의 목표
- Vite + React + Tailwind CSS 프로젝트 세팅
- ⑦ SafetyModal(1393 위기상담) 구현
- ① ProfilePage(반려동물 프로필 입력) 구현
- 공통 컴포넌트 및 API 인스턴스 구축

### 한 일
- [x] Vite + React 18 + Tailwind CSS 프로젝트 초기 세팅 (`frontend/`)
- [x] `react-router-dom`, `axios` 설치 및 기본 라우터 구성 (`App.jsx`)
- [x] **⑦ SafetyModal** 구현 — `fixed` 오버레이 + 1393 `tel:` 링크 버튼 (번호 변경 금지)
- [x] **공통 컴포넌트** 3종 작성
  - `Button.jsx` — primary / danger / ghost variant
  - `Card.jsx` — 흰 배경 + 둥근 모서리 카드
  - `LoadingSpinner.jsx` — 보라색 스피너 + 선택 메시지
- [x] **API 기반 파일** 작성
  - `api/axiosInstance.js` — ngrok baseURL 공통 설정
  - `api/mock.js` — 백엔드 미연동 시 Mock 데이터
  - `api/pets.js` — `createPet()` / `POST /api/v1/pets`
- [x] **① ProfilePage** 구현
  - 이름 / 종(강아지·고양이·기타 라디오) / 기간(날짜 2개) / 추억 키워드(엔터 추가·최대 3개) / 사진 업로드(선택)
  - 제출 시 `POST /api/v1/pets` 호출 → `pet_id`를 `localStorage`에 저장 → `/emotion`으로 이동
  - 실패 시 에러 메시지, 로딩 중 스피너 표시
- [x] `App.jsx` 라우터 전체 경로 설정 (`/profile`, `/emotion`, `/message` 등 플레이스홀더 포함)

### 막힌 것 / 고민
- 백엔드 `POST /api/v1/pets` 스키마(특히 `period` 포맷)가 아직 미확인 → 현재 `"YYYY-MM-DD ~ YYYY-MM-DD"` 문자열로 보내는 중, 백엔드 확인 후 조정 필요
- 사진 업로드는 현재 `file` 객체만 받고 서버 전송은 미구현 → MultipartForm 처리 2주차에 추가 예정

### 내일 할 일
- [ ] ② EmotionPage — 감정 선택(이모지 5개) + 메모 입력
- [ ] SafetyModal과 EmotionPage 연결 (risk_flag 또는 '너무 힘들어요' 선택 시 모달 표시)
- [ ] `api/emotions.js` — `postEmotion()` / `POST /api/v1/emotions`
- [ ] `hooks/useSafetyCheck.js` — 위험 감정 감지 훅
